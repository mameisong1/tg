# 代码审计报告 v3

**审计工程师：** C  
**审计日期：** 2026-04-09 01:04  
**审计目标：** 验证第2轮发现的6个班次相关问题是否已正确修复

---

## 审计结论：✅ PASS

第2轮发现的6个班次相关问题已全部修复，代码实现正确，逻辑完整。

---

## 验证结果

| 问题 | 修复状态 | 验证结果 |
|------|----------|----------|
| 1. 批量修改班次联动水牌状态 | ✅已修复 | ✅正确 |
| 2. 批量修改班次操作日志粒度 | ✅已修复 | ✅正确 |
| 3. 新增助教API支持班次 | ✅已修复 | ✅正确 |
| 4. 编辑助教API支持班次 | ✅已修复 | ✅正确 |
| 5. 后台coaches.html班次字段 | ✅已修复 | ✅正确 |
| 6. init-db.js coaches表shift列 | ✅已修复 | ✅正确 |

---

## 逐项验证详情

### 问题1：批量修改班次联动水牌状态

**文件：** `/TG/tgservice/backend/routes/coaches.js`  
**API：** `PUT /api/coaches/v2/batch-shift`

**验证结果：✅ 正确**

statusMap 映射完整，覆盖6种状态双向映射：
```javascript
const statusMap = {
  '早班空闲': '晚班空闲',
  '晚班空闲': '早班空闲',
  '早班上桌': '晚班上桌',
  '晚班上桌': '早班上桌',
  '早加班': '晚加班',
  '晚加班': '早加班'
};
```

联动逻辑验证：
- ✅ 遍历每个助教，查询原始班次和水牌状态
- ✅ 批量更新 coaches 表的 shift 字段
- ✅ 遍历每个助教，若水牌状态在 statusMap 中则执行联动更新
- ✅ 每个水牌状态变更都记录独立 operationLog（operation_type: '水牌状态变更'）
- ✅ 联动日志 remark 包含班次变更和水牌状态变更信息
- ✅ 汇总日志 remark 包含每个助教详情（如 `李四(A002): 早班→晚班 [水牌已联动]`）
- ✅ 整个操作在事务中执行，失败可回滚

### 问题2：批量修改班次操作日志粒度

**文件：** `/TG/tgservice/backend/routes/coaches.js`  
**API：** `PUT /api/coaches/v2/batch-shift`

**验证结果：✅ 正确**

- ✅ 使用 `changeDetails` 数组记录每个助教的变更详情：
  - `coach_no`：助教编号
  - `stage_name`：艺名
  - `old_shift`：原班次
  - `new_shift`：新班次
  - `water_board_changed`：水牌是否联动变更
- ✅ 汇总日志的 `old_value` 记录每个助教变更前的完整信息
- ✅ 汇总日志的 `new_value` 记录新班次和总人数
- ✅ 汇总日志的 `remark` 包含可读的变更详情字符串（用分号分隔每个助教）

### 问题3：新增助教API支持班次

**文件：** `/TG/tgservice/backend/server.js`  
**API：** `POST /api/admin/coaches`（约第934-961行）

**验证结果：✅ 正确**

- ✅ 解构包含 `shift`：`const { ..., shift } = req.body`
- ✅ 枚举值验证：`validShifts = ['早班', '晚班']`
- ✅ 默认值处理：无效或未传时默认为 `'早班'`
- ✅ INSERT 语句包含 `shift` 字段
- ✅ SQL 参数正确传入 `finalShift`

### 问题4：编辑助教API支持班次

**文件：** `/TG/tgservice/backend/server.js`  
**API：** `PUT /api/admin/coaches/:coachNo`（约第963-1003行）

**验证结果：✅ 正确**

- ✅ 解构包含 `shift`：`const { ..., shift } = req.body`
- ✅ 枚举值验证：`validShifts = ['早班', '晚班']`
- ✅ 原有值保留：先查询当前教练数据 `SELECT ... shift FROM coaches`
- ✅ 最终班次计算：`resolvedShift = finalShift !== undefined ? finalShift : (currentCoach?.shift || '早班')`
- ✅ UPDATE 语句包含 `shift` 字段
- ✅ SQL 参数正确传入 `resolvedShift`

### 问题5：后台coaches.html班次字段

**文件：** `/TG/tgservice/admin/coaches.html`

**验证结果：✅ 正确**

**列表显示：**
- ✅ `renderCoaches()` 中渲染卡片包含班次：
  ```html
  班次: <span style="color:#d4af37;font-weight:600">${c.shift || '早班'}</span>
  ```

**新增/编辑弹窗：**
- ✅ 表单包含班次选择控件：`<select id="formShift">`
- ✅ 选项：早班、晚班
- ✅ `showModal()` 初始化时设置默认值：`formShift.value = '早班'`
- ✅ `editCoach()` 编辑时填充原有值：`formShift.value = c.shift || '早班'`
- ✅ `saveCoach()` 提交时包含班次数据：`shift: document.getElementById('formShift').value`

### 问题6：init-db.js coaches表shift列

**文件：** `/TG/tgservice/backend/init-db.js`（约第105-120行）

**验证结果：✅ 正确**

- ✅ coaches 表 CREATE TABLE 语句包含 `shift` 列：
  ```sql
  shift TEXT DEFAULT '早班',
  ```
- ✅ 列位置正确（在 status 和 created_at 之间）
- ✅ 默认值为 '早班'，与业务逻辑一致

---

## 发现的新问题

**无。** 第2轮发现的6个问题均已正确修复，未发现新的代码问题。

---

## 总结

第2轮审计提出的6个班次相关问题已全部修复：

1. **批量修改班次联动水牌**：statusMap 覆盖6种状态双向映射，联动逻辑正确，每条联动都有独立日志记录
2. **操作日志粒度**：changeDetails 数组记录每个助教的前后班次和水牌变更状态，汇总日志 remark 可读性强
3. **新增助教API**：支持 shift 字段，有枚举验证和默认值
4. **编辑助教API**：支持 shift 字段，有枚举验证和原有值保留逻辑
5. **后台页面**：列表显示班次，弹窗支持班次选择和编辑
6. **数据库Schema**：coaches 表已定义 shift 列，默认值为 '早班'

**所有修复均经过代码级验证，逻辑正确、实现完整，审计通过。**
