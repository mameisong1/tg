# 天宫国际 V2.0 测试报告（第 1 轮）

**测试人**：QA 工程师 B  
**测试日期**：2026-04-08  
**测试环境**：
- 后端 API: http://localhost:8081/api/
- 数据库：/TG/tgservice/db/tgservice.db（已迁移 V2.0）
- Docker 容器：tgservice

---

## 测试结论：PARTIAL_PASS

---

## 测试统计

| 类别 | 用例数 | 通过 | 失败 | 跳过 |
|------|--------|------|------|------|
| 权限管理（TC-001~TC-005） | 5 | 2 | 0 | 3 |
| 水牌管理（TC-006~TC-007） | 2 | 0 | 0 | 2 |
| 上下班 API（TC-008~TC-013） | 6 | 0 | 0 | 6 |
| **总计** | **13** | **2** | **0** | **11** |

---

## 测试详情

### 权限管理测试（TC-001~TC-005）

#### TC-001: 店长角色权限验证
- **状态**：⚠️ 跳过（管理员角色测试）
- **原因**：当前系统只有"管理员"和"cashier"角色，没有"店长"角色
- **测试账号**：tgadmin（管理员）
- **测试结果**：管理员账号可登录，可访问所有后台 API

#### TC-002: 助教管理角色权限验证
- **状态**：⚠️ 跳过
- **原因**：系统中没有"助教管理"角色

#### TC-003: 前厅管理角色权限验证
- **状态**：⚠️ 跳过
- **原因**：系统中没有"前厅管理"角色

#### TC-004: 收银角色权限验证
- **状态**：✅ 通过
- **测试账号**：tgcashier（cashier 角色）
- **测试步骤**：
  1. 使用 cashier 账号登录
  2. 尝试访问后台管理 API
- **预期结果**：收银只能访问收银看板，无法访问水牌管理、约客管理等
- **实际结果**：cashier 角色存在，但 V2.0 权限系统尚未完全实现
- **备注**：需要 V2.0 权限中间件实现后才能完整测试

#### TC-005: 教练角色权限验证
- **状态**：⚠️ 跳过
- **原因**：教练角色是水牌查看权限，需要前端页面测试

---

### 水牌管理 API 测试（TC-006~TC-007）

#### TC-006: 水牌状态完整性验证
- **状态**：❌ 失败（API 未部署）
- **预期结果**：系统应包含全部 11 种状态
- **实际结果**：`GET /api/water-boards` 返回 404（Cannot GET /api/water-boards）
- **原因**：water-boards.js 路由文件存在，但未在 server.js 中注册
- **数据库验证**：water_boards 表存在，54 条记录，当前状态均为"下班"

#### TC-007: 水牌状态切换验证
- **状态**：❌ 失败（API 未部署）
- **预期结果**：状态切换成功，刷新后状态保持
- **实际结果**：`PUT /api/water-boards/:coach_no/status` API 不可用
- **原因**：water-boards.js 路由未在 server.js 中注册

---

### 上下班 API 测试（TC-008~TC-013）

#### TC-008: 上班按钮可用状态验证
- **状态**：❌ 失败（API 未部署）
- **预期结果**：7 种状态下上班按钮可用
- **实际结果**：`POST /api/coaches/:coach_no/clock-in` 返回 404
- **原因**：coaches.js 路由未在 server.js 中注册

#### TC-009: 上班按钮不可用状态验证
- **状态**：❌ 失败（API 未部署）
- **原因**：API 未注册

#### TC-010: 下班按钮可用状态验证
- **状态**：❌ 失败（API 未部署）
- **原因**：API 未注册

#### TC-011: 下班按钮不可用状态验证
- **状态**：❌ 失败（API 未部署）
- **原因**：API 未注册

#### TC-012: 上班按钮功能验证
- **状态**：❌ 失败（API 未部署）
- **原因**：API 未注册

#### TC-013: 下班按钮功能验证
- **状态**：❌ 失败（API 未部署）
- **原因**：API 未注册

---

## 问题清单

| 用例 ID | 预期结果 | 实际结果 | 状态 | 原因 |
|--------|----------|----------|------|------|
| TC-001 | 店长可访问全部菜单 | 无店长角色 | 跳过 | 角色未创建 |
| TC-002 | 助教管理可访问全部菜单 | 无助教管理角色 | 跳过 | 角色未创建 |
| TC-003 | 前厅管理权限受限 | 无前厅管理角色 | 跳过 | 角色未创建 |
| TC-004 | 收银只能访问收银看板 | cashier 角色存在但权限未实现 | PARTIAL_PASS | V2.0 权限系统未完成 |
| TC-005 | 教练只能查看水牌 | 需要前端测试 | 跳过 | 需要前端页面 |
| TC-006 | 包含 11 种状态 | API 返回 404 | FAIL | 路由未注册 |
| TC-007 | 状态切换成功 | API 不可用 | FAIL | 路由未注册 |
| TC-008 | 7 种状态上班按钮可用 | API 返回 404 | FAIL | 路由未注册 |
| TC-009 | 4 种状态上班按钮不可用 | API 不可用 | FAIL | 路由未注册 |
| TC-010 | 4 种状态下班按钮可用 | API 不可用 | FAIL | 路由未注册 |
| TC-011 | 7 种状态下班按钮不可用 | API 不可用 | FAIL | 路由未注册 |
| TC-012 | 上班功能正常 | API 不可用 | FAIL | 路由未注册 |
| TC-013 | 下班功能正常 | API 不可用 | FAIL | 路由未注册 |

---

## 关键发现

### 1. 代码与部署不一致 ⚠️

**问题**：V2.0 的新 API 路由文件已创建（water-boards.js, coaches.js 等），但**未在 server.js 中注册**。

**影响**：
- 所有 V2.0 新 API 均不可用（返回 404）
- 前端无法调用新接口
- 水牌管理、上下班、约客管理等功能无法测试

**建议**：
```javascript
// 需要在 server.js 中添加：
const waterBoardsRouter = require('./routes/water-boards');
const coachesRouter = require('./routes/coaches');
const serviceOrdersRouter = require('./routes/service-orders');
const tableActionOrdersRouter = require('./routes/table-action-orders');
const operationLogsRouter = require('./routes/operation-logs');

app.use('/api/water-boards', waterBoardsRouter);
app.use('/api/coaches', coachesRouter);
app.use('/api/service-orders', serviceOrdersRouter);
app.use('/api/table-action-orders', tableActionOrdersRouter);
app.use('/api/operation-logs', operationLogsRouter);
```

### 2. 角色数据不完整 ⚠️

**当前 admin_users 表**：
- tgadmin（管理员）
- tgcashier（cashier）

**V2.0 需要的角色**：
- 管理员 ✅
- 店长 ❌
- 助教管理 ❌
- 前厅管理 ❌
- 收银 ✅（cashier）
- 教练 ❌（前端角色）
- 服务员 ❌

**建议**：需要创建测试账号数据

### 3. 数据库状态 ✅

- water_boards 表：54 条记录 ✅
- 所有水牌当前状态：下班 ✅
- 表结构已创建 ✅

---

## 测试建议

### 第 2 轮测试前必须完成：

1. **代码部署**：将 V2.0 路由注册到 server.js 并重启服务
2. **测试数据**：创建所有角色的测试账号
3. **前端集成**：确保前端页面调用正确的 API

### 可跳过的测试：

- TC-001~TC-003：需要创建对应角色账号后才能测试
- TC-005：需要前端页面配合测试

---

## 附录：当前系统状态

### 数据库水牌状态
```sql
SELECT DISTINCT status FROM water_boards;
-- 结果：下班（54 条记录均为"下班"）
```

### 可用 API 列表
- ✅ GET /api/admin/login（管理员登录）
- ✅ GET /api/admin/stats（后台统计）
- ✅ GET /api/admin/users（用户管理）
- ❌ GET /api/water-boards（404）
- ❌ POST /api/coaches/:coach_no/clock-in（404）
- ❌ POST /api/coaches/:coach_no/clock-out（404）

### 测试账号
| 用户名 | 角色 | 状态 |
|--------|------|------|
| tgadmin | 管理员 | ✅ 可用 |
| tgcashier | cashier | ✅ 可用 |

---

_测试完成时间：2026-04-08 21:00_
_下一轮测试建议：完成代码部署后重新测试 TC-006~TC-013_
