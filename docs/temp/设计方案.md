# 天宫国际 V2.0 设计方案

**版本**: V2.0  
**日期**: 2026-04-08  
**作者**: 开发工程师 A

---

## 1. 数据库 DDL（SQLite 语法）

### 1.1 admin_users - 后台用户表（修改）

```sql
-- 修改现有 admin_users 表，新增字段
ALTER TABLE admin_users ADD COLUMN name TEXT;  -- 姓名
ALTER TABLE admin_users ADD COLUMN role TEXT DEFAULT '普通员工';  -- 权限角色

-- 角色枚举值：管理员/店长/助教管理/教练/前厅管理/收银/服务员
-- 注意：SQLite 的 ALTER TABLE 不支持修改现有列，如需修改需重建表
```

**完整重建语句（如需全新创建）**:

```sql
CREATE TABLE IF NOT EXISTS admin_users (
    username TEXT PRIMARY KEY,  -- 手机号
    password TEXT NOT NULL,
    name TEXT,  -- 姓名（新增）
    role TEXT DEFAULT '普通员工',  -- 权限角色（新增）
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 1.2 water_boards - 水牌表（新增）

```sql
CREATE TABLE IF NOT EXISTS water_boards (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    coach_no TEXT NOT NULL,  -- 助教工号
    stage_name TEXT NOT NULL,  -- 艺名
    status TEXT DEFAULT '下班',  -- 水牌状态
    table_no TEXT,  -- 台桌号（上桌时填写）
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (coach_no) REFERENCES coaches(coach_no)
);

-- 状态枚举值：早班上桌/早班空闲/晚班上桌/晚班空闲/早加班/晚加班/休息/公休/请假/乐捐/下班
CREATE INDEX idx_water_boards_status ON water_boards(status);
CREATE INDEX idx_water_boards_coach_no ON water_boards(coach_no);
```

---

### 1.3 service_orders - 服务单表（新增）

```sql
CREATE TABLE IF NOT EXISTS service_orders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_time DATETIME DEFAULT CURRENT_TIMESTAMP,  -- 下单时间
    table_no TEXT NOT NULL,  -- 台桌号
    requirement TEXT NOT NULL,  -- 需求
    requester_name TEXT NOT NULL,  -- 下单人（助教艺名或后台用户姓名）
    requester_type TEXT DEFAULT '助教',  -- 下单人类型：助教/后台用户
    status TEXT DEFAULT '待处理',  -- 状态：待处理/已完成/已取消
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_service_orders_status ON service_orders(status);
CREATE INDEX idx_service_orders_table_no ON service_orders(table_no);
CREATE INDEX idx_service_orders_created_at ON service_orders(created_at);
```

---

### 1.4 table_action_orders - 上下桌单表（新增）

```sql
CREATE TABLE IF NOT EXISTS table_action_orders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_time DATETIME DEFAULT CURRENT_TIMESTAMP,  -- 下单时间
    table_no TEXT NOT NULL,  -- 台桌号
    coach_no TEXT NOT NULL,  -- 助教工号
    order_type TEXT NOT NULL,  -- 单类型：上桌单/下桌单/取消单
    action_category TEXT,  -- 上桌类别：普通课/标签课（仅上桌单需要）
    stage_name TEXT NOT NULL,  -- 助教艺名
    status TEXT DEFAULT '待处理',  -- 状态：待处理/已完成/已取消
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (coach_no) REFERENCES coaches(coach_no)
);

CREATE INDEX idx_table_action_orders_type ON table_action_orders(order_type);
CREATE INDEX idx_table_action_orders_status ON table_action_orders(status);
CREATE INDEX idx_table_action_orders_coach_no ON table_action_orders(coach_no);
CREATE INDEX idx_table_action_orders_created_at ON table_action_orders(created_at);
```

---

### 1.5 applications - 申请事项表（新增）

```sql
CREATE TABLE IF NOT EXISTS applications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    applicant_phone TEXT NOT NULL,  -- 申请人手机号
    application_type TEXT NOT NULL,  -- 申请类型：早加班申请/晚加班申请/公休申请/乐捐报备/约客记录
    remark TEXT,  -- 备注
    proof_image_url TEXT,  -- 证明图片 URL（加班截图/约客截图等）
    status INTEGER DEFAULT 0,  -- 状态：0 待处理/1 同意或有效/2 拒绝或无效
    approver_phone TEXT,  -- 审批人手机号
    approve_time DATETIME,  -- 审批时间
    extra_data TEXT,  -- 额外数据（JSON 格式，如乐捐的小时数等）
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_applications_type ON applications(application_type);
CREATE INDEX idx_applications_status ON applications(status);
CREATE INDEX idx_applications_applicant ON applications(applicant_phone);
CREATE INDEX idx_applications_created_at ON applications(created_at);
```

---

### 1.6 coaches - 助教表（修改）

```sql
-- 新增班次字段
ALTER TABLE coaches ADD COLUMN shift TEXT DEFAULT '早班';  -- 班次：早班/晚班

-- 完整表结构（重建用）
CREATE TABLE IF NOT EXISTS coaches (
    coach_no TEXT PRIMARY KEY,
    employee_id TEXT,
    stage_name TEXT,
    real_name TEXT,
    id_card_last6 TEXT,
    level TEXT DEFAULT '初级',
    price REAL DEFAULT 2.3,
    age INTEGER,
    height INTEGER,
    photos TEXT,  -- JSON 数组
    video TEXT,
    intro TEXT,
    is_popular INTEGER DEFAULT 0,
    shift TEXT DEFAULT '早班',  -- 班次（新增）
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 1.7 guest_invitation_results - 约客结果表（新增）

```sql
CREATE TABLE IF NOT EXISTS guest_invitation_results (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT NOT NULL,  -- 日期（YYYY-MM-DD 格式）
    shift TEXT NOT NULL,  -- 早班/晚班
    coach_no TEXT NOT NULL,  -- 助教工号
    stage_name TEXT NOT NULL,  -- 艺名
    invitation_image_url TEXT,  -- 约客记录图片 URL
    result TEXT DEFAULT '未约客',  -- 结果：未约客/约客有效/约客无效
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (coach_no) REFERENCES coaches(coach_no),
    UNIQUE(date, shift, coach_no)  -- 每天每班每个助教只有一条记录
);

CREATE INDEX idx_guest_invitation_date ON guest_invitation_results(date);
CREATE INDEX idx_guest_invitation_shift ON guest_invitation_results(shift);
CREATE INDEX idx_guest_invitation_coach_no ON guest_invitation_results(coach_no);
```

---

## 2. API 接口设计

### 2.1 后台用户管理 API

#### 2.1.1 创建后台用户
```
POST /api/admin/users
Content-Type: application/json

请求体:
{
  "username": "13800138000",  // 手机号
  "password": "randomPassword123",  // 初始密码（随机生成）
  "name": "张三",  // 姓名
  "role": "店长"  // 角色：管理员/店长/助教管理/教练/前厅管理/收银/服务员
}

响应:
{
  "success": true,
  "data": { "username": "13800138000", "role": "店长" }
}
```

#### 2.1.2 获取用户权限
```
GET /api/admin/users/:username/permissions

响应:
{
  "success": true,
  "data": {
    "username": "13800138000",
    "name": "张三",
    "role": "店长",
    "permissions": ["all"]  // 权限列表
  }
}
```

#### 2.1.3 更新用户信息
```
PUT /api/admin/users/:username
Content-Type: application/json

请求体:
{
  "name": "李四",
  "role": "前厅管理"
}
```

#### 2.1.4 删除用户
```
DELETE /api/admin/users/:username
```

---

### 2.2 水牌管理 API

#### 2.2.1 获取所有水牌状态
```
GET /api/water-boards

查询参数:
- status: 可选，按状态筛选
- shift: 可选，早班/晚班

响应:
{
  "success": true,
  "data": [
    {
      "coach_no": "26",
      "stage_name": "四瑶",
      "status": "早班上桌",
      "table_no": "普台 1",
      "updated_at": "2026-04-08 10:30:00"
    },
    ...
  ]
}
```

#### 2.2.2 更新水牌状态（手动）
```
PUT /api/water-boards/:coach_no/status
Content-Type: application/json

请求体:
{
  "status": "早班空闲",  // 新状态
  "table_no": null  // 清空台桌号（可选）
}
```

#### 2.2.3 获取单个助教水牌
```
GET /api/water-boards/:coach_no
```

---

### 2.3 服务单 API

#### 2.3.1 创建服务单
```
POST /api/service-orders
Content-Type: application/json

请求体:
{
  "table_no": "普台 1",
  "requirement": "换垃圾袋",  // 或自定义文本
  "requester_name": "四瑶",
  "requester_type": "助教"  // 助教/后台用户
}

响应:
{
  "success": true,
  "data": { "id": 1, "status": "待处理" }
}
```

#### 2.3.2 获取服务单列表
```
GET /api/service-orders

查询参数:
- status: 待处理/已完成/已取消
- table_no: 可选
- limit: 默认 50

响应:
{
  "success": true,
  "data": [
    {
      "id": 1,
      "order_time": "2026-04-08 10:30:00",
      "table_no": "普台 1",
      "requirement": "换垃圾袋",
      "requester_name": "四瑶",
      "requester_type": "助教",
      "status": "待处理",
      "created_at": "2026-04-08 10:30:00"
    },
    ...
  ]
}
```

#### 2.3.3 更新服务单状态
```
PUT /api/service-orders/:id/status
Content-Type: application/json

请求体:
{
  "status": "已完成"  // 待处理/已完成/已取消
}
```

---

### 2.4 上下桌单 API

#### 2.4.1 提交上桌单
```
POST /api/table-action-orders
Content-Type: application/json

请求体:
{
  "table_no": "普台 1",
  "coach_no": "26",
  "order_type": "上桌单",
  "action_category": "普通课",  // 普通课/标签课
  "stage_name": "四瑶"
}

响应:
{
  "success": true,
  "data": { "id": 1, "status": "待处理" }
}
```

#### 2.4.2 提交下桌单
```
POST /api/table-action-orders
Content-Type: application/json

请求体:
{
  "table_no": "普台 1",
  "coach_no": "26",
  "order_type": "下桌单",
  "stage_name": "四瑶"
}
```

#### 2.4.3 提交取消单
```
POST /api/table-action-orders
Content-Type: application/json

请求体:
{
  "table_no": "普台 1",
  "coach_no": "26",
  "order_type": "取消单",
  "stage_name": "四瑶"
}
```

#### 2.4.4 获取上下桌单列表
```
GET /api/table-action-orders

查询参数:
- status: 待处理/已完成/已取消
- order_type: 上桌单/下桌单/取消单
- coach_no: 可选
- limit: 默认 50
```

#### 2.4.5 更新上下桌单状态
```
PUT /api/table-action-orders/:id/status
Content-Type: application/json

请求体:
{
  "status": "已完成"
}
```

---

### 2.5 申请事项 API

#### 2.5.1 提交加班申请
```
POST /api/applications
Content-Type: application/json

请求体:
{
  "applicant_phone": "13800138000",
  "application_type": "早加班申请",  // 早加班申请/晚加班申请
  "remark": "昨晚通宵加班",
  "proof_image_url": "https://oss.xxx.com/proof/xxx.jpg"
}

响应:
{
  "success": true,
  "data": { "id": 1, "status": 0 }
}
```

#### 2.5.2 提交公休申请
```
POST /api/applications
Content-Type: application/json

请求体:
{
  "applicant_phone": "13800138000",
  "application_type": "公休申请",
  "remark": "通宵加班后公休",
  "proof_image_url": "https://oss.xxx.com/proof/xxx.jpg"
}
```

#### 2.5.3 提交乐捐报备
```
POST /api/applications
Content-Type: application/json

请求体:
{
  "applicant_phone": "13800138000",
  "application_type": "乐捐报备",
  "remark": "和客人外出",
  "extra_data": {
    "date": "2026-04-08",
    "hours": 3  // 外出小时数
  }
}

注意：乐捐报备不需要审批，提交后状态自动设为 1（有效）
```

#### 2.5.4 提交约客记录
```
POST /api/applications
Content-Type: application/json

请求体:
{
  "applicant_phone": "13800138000",
  "application_type": "约客记录",
  "proof_image_url": "https://oss.xxx.com/invitation/xxx.jpg"
}
```

#### 2.5.5 审批申请事项
```
PUT /api/applications/:id/approve
Content-Type: application/json

请求体:
{
  "approver_phone": "13900139000",  // 审批人手机号
  "status": 1  // 1 同意/2 拒绝
}
```

#### 2.5.6 获取申请事项列表
```
GET /api/applications

查询参数:
- application_type: 可选
- status: 0/1/2
- applicant_phone: 可选
- approver_phone: 可选
- limit: 默认 50
```

#### 2.5.7 获取乐捐报备一览
```
GET /api/applications/lejuan

查询参数:
- days: 最近 N 天，默认 10

响应:
{
  "success": true,
  "data": [
    {
      "id": 1,
      "applicant_phone": "13800138000",
      "stage_name": "四瑶",
      "date": "2026-04-08",
      "hours": 3,
      "created_at": "2026-04-08 14:00:00"
    },
    ...
  ]
}
```

---

### 2.6 助教管理 API

#### 2.6.1 批量修改班次
```
PUT /api/coaches/batch-shift
Content-Type: application/json

请求体:
{
  "coach_no_list": ["26", "18", "12"],
  "shift": "早班"  // 早班/晚班
}

响应:
{
  "success": true,
  "data": { "updated_count": 3 }
}
```

#### 2.6.2 助教上班
```
POST /api/coaches/:coach_no/clock-in

响应: 自动根据班次设置水牌为早班空闲/晚班空闲
```

#### 2.6.3 助教下班
```
POST /api/coaches/:coach_no/clock-out

响应: 设置水牌为下班状态
```

---

### 2.7 约客管理 API

#### 2.7.1 提交约客记录（前端直接上传 OSS）
```
POST /api/guest-invitations
Content-Type: application/json

请求体:
{
  "coach_no": "26",
  "date": "2026-04-08",
  "shift": "早班",  // 早班/晚班
  "invitation_image_url": "https://oss.xxx.com/invitation/xxx.jpg"
}

响应:
{
  "success": true,
  "data": { "id": 1, "result": "未约客" }
}
```

#### 2.7.2 审查约客记录
```
PUT /api/guest-invitations/:id/review
Content-Type: application/json

请求体:
{
  "result": "约客有效"  // 约客有效/约客无效
}
```

#### 2.7.3 生成约客统计
```
POST /api/guest-invitations/statistics
Content-Type: application/json

请求体:
{
  "date": "2026-04-08",
  "shift": "早班"  // 早班/晚班
}

响应:
{
  "success": true,
  "data": {
    "date": "2026-04-08",
    "shift": "早班",
    "should_invite_count": 10,  // 应约客人数
    "invited_count": 8,  // 已约客人数
    "invalid_list": [  // 无效约客助教一览
      { "coach_no": "18", "stage_name": "小美" }
    ],
    "missing_list": [  // 未约客助教一览
      { "coach_no": "12", "stage_name": "小丽" }
    ]
  }
}
```

#### 2.7.4 获取约客统计结果
```
GET /api/guest-invitations/statistics/:date/:shift
```

---

### 2.8 收银看板 API

#### 2.8.1 获取看板数据
```
GET /api/cashier-dashboard

查询参数:
- tab: pending/completed/cancelled  // 待处理/已完成/已取消
- hours: 2  // 时间范围（已完成/已取消 显示最近 N 小时）

响应:
{
  "success": true,
  "data": {
    "orders": [  // 商品订单
      { "id": 1, "order_no": "TG20260408100000001", "table_no": "普台 1", ... }
    ],
    "service_orders": [  // 服务单
      { "id": 1, "table_no": "普台 1", "requirement": "换垃圾袋", ... }
    ],
    "table_action_orders": [  // 上下桌单
      { "id": 1, "table_no": "普台 1", "order_type": "上桌单", ... }
    ]
  }
}
```

---

## 3. 水牌状态转换表

### 3.1 水牌状态枚举（11 种）

| 状态 ID | 状态名称 | 说明 |
|---------|----------|------|
| 1 | 早班上桌 | 早班助教正在台上服务 |
| 2 | 早班空闲 | 早班助教可用 |
| 3 | 晚班上桌 | 晚班助教正在台上服务 |
| 4 | 晚班空闲 | 晚班助教可用 |
| 5 | 早加班 | 早班加班状态 |
| 6 | 晚加班 | 晚班加班状态 |
| 7 | 休息 | 手动设置休息 |
| 8 | 公休 | 审批通过的公休 |
| 9 | 请假 | 手动设置请假 |
| 10 | 乐捐 | 和客人外出 |
| 11 | 下班 | 已下班 |

---

### 3.2 状态转换矩阵

| 当前状态 | 触发事件 | 目标状态 | 自动/手动 | 说明 |
|----------|----------|----------|-----------|------|
| **下班** | 上班按钮 | 早班空闲/晚班空闲 | 自动 | 根据助教表.班次决定 |
| **早加班** | 上班按钮 | 早班空闲 | 自动 | - |
| **晚加班** | 上班按钮 | 晚班空闲 | 自动 | - |
| **休息** | 上班按钮 | 早班空闲/晚班空闲 | 自动 | 根据班次决定 |
| **公休** | 上班按钮 | 早班空闲/晚班空闲 | 自动 | 根据班次决定 |
| **请假** | 上班按钮 | 早班空闲/晚班空闲 | 自动 | 根据班次决定 |
| **乐捐** | 上班按钮 | 早班空闲/晚班空闲 | 自动 | 根据班次决定 |
| **早班空闲** | 下班按钮 | 下班 | 自动 | - |
| **晚班空闲** | 下班按钮 | 下班 | 自动 | - |
| **早班上桌** | 下班按钮 | 下班 | 自动 | - |
| **晚班上桌** | 下班按钮 | 下班 | 自动 | - |
| **早班空闲** | 上桌单 | 早班上桌 | 自动 | 录入台桌号 |
| **晚班空闲** | 上桌单 | 晚班上桌 | 自动 | 录入台桌号 |
| **早班上桌** | 下桌单 | 早班空闲 | 自动 | 清空台桌号 |
| **晚班上桌** | 下桌单 | 晚班空闲 | 自动 | 清空台桌号 |
| **早班上桌** | 取消单 | 早班空闲 | 自动 | 清空台桌号 |
| **晚班上桌** | 取消单 | 晚班空闲 | 自动 | 清空台桌号 |
| **早班空闲** | 加班审批通过 | 早加班 | 自动 | 早加班申请 → 早加班 |
| **晚班空闲** | 加班审批通过 | 晚加班 | 自动 | 晚加班申请 → 晚加班 |
| **任意状态** | 公休审批通过 | 公休 | 自动 | - |
| **任意状态** | 乐捐报备提交 | 乐捐 | 自动 | 无需审批 |
| **任意状态** | 手动设置休息 | 休息 | 手动 | 助教管理操作 |
| **任意状态** | 手动设置请假 | 请假 | 手动 | 助教管理操作 |

---

### 3.3 状态转换流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        上班流程                                  │
├─────────────────────────────────────────────────────────────────┤
│  下班 → 上班按钮 → [判断班次] → 早班空闲 / 晚班空闲              │
│  早加班 → 上班按钮 → 早班空闲                                    │
│  晚加班 → 上班按钮 → 晚班空闲                                    │
│  休息 → 上班按钮 → [判断班次] → 早班空闲 / 晚班空闲              │
│  公休 → 上班按钮 → [判断班次] → 早班空闲 / 晚班空闲              │
│  请假 → 上班按钮 → [判断班次] → 早班空闲 / 晚班空闲              │
│  乐捐 → 上班按钮 → [判断班次] → 早班空闲 / 晚班空闲              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        下班流程                                  │
├─────────────────────────────────────────────────────────────────┤
│  早班空闲 → 下班按钮 → 下班                                      │
│  晚班空闲 → 下班按钮 → 下班                                      │
│  早班上桌 → 下班按钮 → 下班                                      │
│  晚班上桌 → 下班按钮 → 下班                                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        上下桌流程                                │
├─────────────────────────────────────────────────────────────────┤
│  早班空闲 → 上桌单 → 早班上桌（录入台桌号）                      │
│  晚班空闲 → 上桌单 → 晚班上桌（录入台桌号）                      │
│  早班上桌 → 下桌单 → 早班空闲（清空台桌号）                      │
│  晚班上桌 → 下桌单 → 晚班空闲（清空台桌号）                      │
│  早班上桌 → 取消单 → 早班空闲（清空台桌号）                      │
│  晚班上桌 → 取消单 → 晚班空闲（清空台桌号）                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        申请流程                                  │
├─────────────────────────────────────────────────────────────────┤
│  早加班申请 → 审批通过 → 早加班                                  │
│  晚加班申请 → 审批通过 → 晚加班                                  │
│  公休申请 → 审批通过 → 公休                                      │
│  乐捐报备 → 提交 → 乐捐（无需审批）                              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        手动操作                                  │
├─────────────────────────────────────────────────────────────────┤
│  任意状态 → 助教管理手动设置 → 休息                              │
│  任意状态 → 助教管理手动设置 → 请假                              │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.4 状态按钮可用性矩阵

| 当前状态 | 上班按钮 | 下班按钮 | 上桌单 | 下桌单 | 取消单 |
|----------|----------|----------|--------|--------|--------|
| 早班上桌 | ❌ | ✅ | ❌ | ✅ | ✅ |
| 早班空闲 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 晚班上桌 | ❌ | ✅ | ❌ | ✅ | ✅ |
| 晚班空闲 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 早加班 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 晚加班 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 休息 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 公休 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 请假 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 乐捐 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 下班 | ✅ | ❌ | ❌ | ❌ | ❌ |

---

## 4. 权限矩阵

### 4.1 后台权限

| 角色 | 收银看板 | 商品管理 | 包房管理 | 助教管理 | 水牌管理 | 约客审查 | 全部菜单 |
|------|----------|----------|----------|----------|----------|----------|----------|
| 管理员 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 店长 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 助教管理 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 教练 | ✅ | ❌ | ❌ | ❌ | 只读 | ❌ | ❌ |
| 前厅管理 | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 收银 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 服务员 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

### 4.2 前台【内部专用】版块权限

| 角色 | 个人中心 | 上班 | 下班 | 上桌单 | 下桌单 | 取消单 | 加班申请 | 公休申请 | 乐捐报备 | 上传约客 |
|------|----------|------|------|--------|--------|--------|----------|----------|----------|----------|
| 助教 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 店长 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 助教管理 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 教练 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 其他后台用户 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

| 角色 | 加班审批 | 公休审批 | 乐捐一览 | 约客审查 | 约客统计 | 水牌管理 |
|------|----------|----------|----------|----------|----------|----------|
| 助教 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 店长 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 助教管理 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 教练 | ❌ | ❌ | ❌ | ❌ | ❌ | 只读 |
| 其他后台用户 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

---

## 5. 前端页面清单

### 5.1 后台页面

| 页面 | 路径 | 权限 | 说明 |
|------|------|------|------|
| 收银看板 | /admin/cashier-dashboard | 收银/前厅管理/店长/管理员 | 全屏显示，去头 |
| 商品管理 | /admin/products | 前厅管理/店长/管理员 | - |
| 包房管理 | /admin/vip-rooms | 前厅管理/店长/管理员 | - |
| 助教管理 | /admin/coaches | 助教管理/店长/管理员 | 含批量修改班次 |
| 水牌管理 | /admin/water-boards | 助教管理/店长/管理员 | 手动调整状态 |
| 加班审批 | /admin/overtime-approval | 助教管理/店长 | - |
| 公休审批 | /admin/leave-approval | 助教管理/店长 | - |
| 乐捐报备一览 | /admin/lejuan-list | 助教管理/店长 | 最近 10 天 |
| 约客审查（早班） | /admin/invitation-review-morning | 助教管理/店长 | 16:00 后开放 |
| 约客审查（晚班） | /admin/invitation-review-evening | 助教管理/店长 | 20:00 后开放 |
| 约客统计（早班） | /admin/invitation-stats-morning | 助教管理/店长 | 审查完毕后开放 |
| 约客统计（晚班） | /admin/invitation-stats-evening | 助教管理/店长 | 审查完毕后开放 |

### 5.2 前台页面（H5/小程序）

| 页面 | 路径 | 权限 | 说明 |
|------|------|------|------|
| 内部专用首页 | /internal | 后台用户/助教 | 入口页面 |
| 个人中心 | /internal/profile | 助教 | 原有功能 |
| 上班/下班 | /internal/clock | 助教 | 按钮根据状态灰显 |
| 上桌单 | /internal/table-in | 助教 | 选择台桌号+类型 |
| 下桌单 | /internal/table-out | 助教 | 确认下桌 |
| 取消单 | /internal/table-cancel | 助教 | 确认取消 |
| 加班申请 | /internal/overtime-apply | 助教 | 上传截图 |
| 公休申请 | /internal/leave-apply | 助教 | 上传截图 |
| 乐捐报备 | /internal/lejuan | 助教 | 输入日期+小时数 |
| 上传约客记录 | /internal/invitation-upload | 助教 | 直接上传 OSS |
| 服务下单 | /internal/service-order | 所有后台用户 | 桌号 + 需求 |
| 水牌查看 | /internal/water-boards-view | 教练 | 只读 |
| 水牌管理 | /internal/water-boards | 助教管理/店长 | 可编辑 |

---

## 6. OSS 上传目录配置

```javascript
// .config 配置文件
oss: {
  tmpDir: 'tmp/',  // 临时上传目录（加班截图/约客截图）
  coachDir: 'coaches/',  // 助教照片目录
  productDir: 'products/',  // 商品图片目录
  // ...
}
```

---

## 7. 定时任务

### 7.1 约客审查时间检查

- **早班约客审查**: 每天 16:00 开放
- **晚班约客审查**: 每天 20:00 开放
- **约客统计开放条件**:
  - 早班：16:00 后 + 所有早班助教约客记录审查完毕
  - 晚班：20:00 后 + 所有晚班助教约客记录审查完毕

### 7.2 服务单/上下桌单清理

- 已完成/已取消 记录保留 2 小时
- 定时任务每小时清理一次过期记录

---

## 8. 数据迁移脚本

### 8.1 数据库迁移 SQL

```sql
-- 1. 修改 admin_users 表
ALTER TABLE admin_users ADD COLUMN name TEXT;
ALTER TABLE admin_users ADD COLUMN role TEXT DEFAULT '普通员工';

-- 2. 修改 coaches 表
ALTER TABLE coaches ADD COLUMN shift TEXT DEFAULT '早班';

-- 3. 创建新表（见第 1 节 DDL）
-- water_boards, service_orders, table_action_orders, applications, guest_invitation_results

-- 4. 初始化水牌表（从现有助教数据）
INSERT INTO water_boards (coach_no, stage_name, status, table_no)
SELECT coach_no, stage_name, '下班', NULL FROM coaches;
```

### 8.2 默认后台用户创建

```sql
-- 店长
INSERT INTO admin_users (username, password, name, role) 
VALUES ('13800138000', 'bcrypt_hash', '张店长', '店长');

-- 助教管理
INSERT INTO admin_users (username, password, name, role) 
VALUES ('13800138001', 'bcrypt_hash', '李管理', '助教管理');

-- 收银
INSERT INTO admin_users (username, password, name, role) 
VALUES ('13800138002', 'bcrypt_hash', '王收银', '收银');
```

---

## 9. 注意事项

1. **状态转换原子性**: 水牌状态更新必须与上下桌单创建在同一事务中
2. **权限校验**: 所有 API 必须校验用户权限
3. **OSS 直传**: 前端直接上传 OSS，后端只存储 URL
4. **并发控制**: 水牌状态更新需要加锁，避免并发冲突
5. **数据备份**: 新增表需要加入每日备份脚本
6. **日志记录**: 所有状态变更需要记录操作日志

---

*文档结束*
