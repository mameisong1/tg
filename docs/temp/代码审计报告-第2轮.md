# 天宫国际 V2.0 代码审计报告 - 第2轮

**审计日期**: 2026-04-08  
**审计工程师**: C  
**审计范围**: 第1轮修订内容验证 + 新问题检查  
**参考报告**: `/TG/docs/temp/代码审计报告.md`

---

## 审计结论：NEED_REVISION

第1轮修订的4个严重问题已全部修复，但引入了1个新代码质量问题，且第1轮审计报告中提出的中等问题（上班 API 状态校验、并发控制、硬编码密钥）未修复。

---

## 修订验证

### ✅ 问题1：删除 applications.js 中的约客记录类型 - 已修复

**验证结果**: 
- `applications.js` 第 34-42 行的 `validTypes` 数组现在只包含：
  ```javascript
  const validTypes = [
    '早加班申请',
    '晚加班申请',
    '公休申请',
    '乐捐报备'
  ];
  ```
- 约客记录提交已统一使用 `/api/guest-invitations` 接口
- **修复正确** ✅

---

### ✅ 问题2：SQL 注入修复为参数化查询 - 已修复

**验证结果**:
- `applications.js` 第 200-206 行（乐捐报备一览接口）：
  ```javascript
  const daysNum = parseInt(days, 10);
  const applications = await db.all(`
    SELECT a.*, c.stage_name
    FROM applications a
    LEFT JOIN coaches c ON a.applicant_phone = c.employee_id OR a.applicant_phone = c.phone
    WHERE a.application_type = '乐捐报备'
      AND a.status = 1
      AND date(a.created_at) >= date('now', ?)
  `, ['-' + daysNum + ' days']);
  ```
- 使用参数化查询 `date('now', ?)` 替代字符串拼接
- **修复正确** ✅

---

### ✅ 问题3：加班/公休审批增加状态校验 - 已修复

**验证结果**:
- `applications.js` 第 273-283 行增加了状态校验：
  ```javascript
  // 状态转换校验：检查当前状态是否允许转换
  const currentStatus = currentWaterBoard.status;
  const isOnTable = currentStatus === '早班上桌' || currentStatus === '晚班上桌';
  
  // 如果助教正在上桌服务，不允许审批通过加班/公休申请
  if (isOnTable) {
    await transaction.rollback();
    return res.status(400).json({
      success: false,
      error: `助教${coach.stage_name}正在上桌服务（${currentStatus}），无法审批通过${application.application_type}`
    });
  }
  ```
- 符合设计方案 3.2 节状态转换矩阵要求
- **修复正确** ✅

---

### ✅ 问题4：约客统计增加审查完毕校验 - 已修复（存在代码质量问题）

**验证结果**:
- `guest-invitations.js` 第 268-280 行增加了审查完毕校验：
  ```javascript
  // 校验所有记录是否审查完毕
  const shouldInviteStatus = shift === '早班'
    ? ['早班空闲', '早班上桌']
    : ['晚班空闲', '晚班上桌'];
  const placeholders = shouldInviteStatus.map(() => '?').join(',');
  
  const allInvitations = await transaction.all(`
    SELECT gir.result
    FROM guest_invitation_results gir
    INNER JOIN coaches c ON gir.coach_no = c.coach_no
    INNER JOIN water_boards wb ON wb.coach_no = c.coach_no
    WHERE gir.date = ? AND gir.shift = ? AND wb.status IN (${placeholders})
  `, [date, shift, ...shouldInviteStatus]);
  
  const hasUnreviewed = allInvitations.some(inv => inv.result === '待审查');
  if (hasUnreviewed) {
    return res.status(400).json({
      success: false,
      error: '尚有约客记录未审查完毕，无法生成统计'
    });
  }
  ```
- **修复正确** ✅

**⚠️ 新引入问题**:
- 第 283-285 行**重复定义**了 `shouldInviteStatus` 变量：
  ```javascript
  // 应约客状态定义
  const shouldInviteStatus = shift === '早班'
    ? ['早班空闲', '早班上桌']
    : ['晚班空闲', '晚班上桌'];
  ```
- 这会导致代码冗余和潜在维护问题
- **建议**: 删除第 283-285 行的重复定义

---

## 剩余问题

### 🔴 严重问题（无）

第1轮的4个严重问题已全部修复。

---

### 🟡 中等问题（需要修订）

#### 1. guest-invitations.js 变量重复定义（新引入）

**问题描述**: 
- `guest-invitations.js` 第 268-270 行和第 283-285 行重复定义了 `shouldInviteStatus` 变量
- 虽然功能上不影响，但属于代码质量问题

**位置**: `/TG/tgservice/backend/routes/guest-invitations.js:283-285`

**严重程度**: 🟡 中（代码质量）

**修复建议**: 删除第 283-285 行的重复定义

---

#### 2. 上班 API 未完全遵循状态转换矩阵（第1轮报告中问题6，未修复）

**问题描述**: 
- `coaches.js` 第 43-50 行：当助教已在班状态（早班空闲/晚班空闲）时，代码返回 `success: true` 和 `warning`，而非 400 错误
- 设计方案 3.4 节规定"早班空闲/晚班空闲"状态的上班按钮应该灰显（不可用）
- API 层面应拒绝已在班状态的上班请求

**位置**: `/TG/tgservice/backend/routes/coaches.js:43-50`

**严重程度**: 🟡 中

**当前代码**:
```javascript
} else if (waterBoard.status === '早班空闲' || waterBoard.status === '晚班空闲') {
  // 已在班状态，返回提示
  return res.json({
    success: true,
    data: {
      coach_no,
      status: waterBoard.status,
      message: '已在班状态'
    },
    warning: '已在班状态'
  });
}
```

**修复建议**: 返回 400 错误：
```javascript
return res.status(400).json({
  success: false,
  error: '助教已在班状态，无需重复上班'
});
```

---

#### 3. 上下桌单 API 缺少并发控制（第1轮报告中问题8，未修复）

**问题描述**: 
- `table-action-orders.js` 第 55-96 行：先查询水牌状态，然后更新状态，中间存在竞态条件
- 如果两个请求同时提交，可能导致状态覆盖

**位置**: `/TG/tgservice/backend/routes/table-action-orders.js:55-96`

**严重程度**: 🟡 中

**修复建议**: 使用乐观锁机制，在更新时检查 `updated_at` 时间戳或版本号

---

#### 4. 认证中间件使用硬编码密钥（第1轮报告中问题11，未修复）

**问题描述**: 
- `auth.js` 第 66 行使用硬编码密钥 `'tgservice-secret-key'`
- 不符合安全最佳实践

**位置**: `/TG/tgservice/backend/middleware/auth.js:66`

**严重程度**: 🟡 中（安全性）

**修复建议**: 从环境变量读取密钥：
```javascript
const JWT_SECRET = process.env.TGSERVICE_SECRET_KEY || 'tgservice-secret-key';
```

---

### 🟢 低等问题（建议改进）

以下第1轮审计报告中的低等问题未修复，建议后续迭代中逐步改进：

1. **操作日志 old_value/new_value 格式不统一**（问题9）
2. **缺少输入字段的长度校验**（问题12）
3. **错误日志缺少详细上下文**（问题13）

---

## 正面评价

### ✅ 第1轮修订质量高

- 4个严重问题全部修复
- 修复方案符合设计方案要求
- 代码逻辑清晰，注释完整

### ✅ 代码结构保持良好

- 事务处理完整
- 错误处理规范
- 操作日志记录完整

---

## 建议

### 短期（必须完成）

1. **删除 guest-invitations.js 第 283-285 行的重复变量定义**
2. **修改 coaches.js 上班 API，对已在班状态返回 400 错误**

### 中期（建议完成）

3. **实现上下桌单 API 的并发控制机制**
4. **从环境变量读取认证密钥**

### 长期（代码质量改进）

5. 统一操作日志的 JSON 格式
6. 增加输入字段长度校验
7. 增强错误日志的上下文信息

---

## 审计总结

第2轮审计确认第1轮修订的4个严重问题已全部正确修复，代码质量整体提升。但引入了1个变量重复定义的代码质量问题，且第1轮报告中提出的部分中等问题（上班 API 状态校验、并发控制、硬编码密钥）尚未修复。

**建议优先级**：
1. 先修复 guest-invitations.js 的变量重复定义（5 分钟工作量）
2. 修复 coaches.js 上班 API 的状态校验（10 分钟工作量）
3. 逐步改进并发控制和安全性问题

---

_审计完成日期：2026-04-08_
