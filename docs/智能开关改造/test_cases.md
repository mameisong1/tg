# 天宫智能开关改造 - 测试用例

> 文档版本：v1.1 | 创建日期：2026-04-14 | 修订日期：2026-04-14 | 负责人：测试B
> 修订记录：v1.0→v1.1 根据审计C审计报告补充定时自动开灯测试、修正台桌设备表字段、移除开关场景表Update测试、增强跨午夜边界测试、补充空集合/MQTT容错/延迟配置测试

---

## 一、数据库表测试

### TC-001：设备开关表 - 正常创建记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-001 |
| **测试标题** | 设备开关表正常插入一条完整记录 |
| **前置条件** | 数据库已创建 `设备开关表`，表结构包含：开关ID、开关序号、开关标签、自动关灯开始时间、自动关灯结束时间 |
| **测试步骤** | 1. 执行 INSERT 语句，插入一条合法记录：<br>`开关ID='0xa4c1380bbd942cbe'`<br>`开关序号='state_l4'`<br>`开关标签='吊灯-01'`<br>`自动关灯开始时间='22:00'`<br>`自动关灯结束时间='06:00'`<br>2. 执行 SELECT 查询该记录 |
| **预期结果** | INSERT 成功无报错，SELECT 能查到该记录且各字段值与插入值完全一致 |
| **测试环境** | 开发环境 |

---

### TC-002：设备开关表 - 主键唯一性约束（开关ID+开关序号）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-002 |
| **测试标题** | 设备开关表主键唯一性约束：相同开关ID+开关序号不允许重复插入 |
| **前置条件** | 设备开关表中已存在记录：`开关ID='0xa4c1380bbd942cbe'`, `开关序号='state_l4'` |
| **测试步骤** | 1. 尝试插入另一条记录，开关ID和开关序号与已有记录完全相同，但开关标签不同（如'吊灯-02'）<br>2. 观察 INSERT 结果 |
| **预期结果** | INSERT 失败，数据库返回唯一约束冲突错误，表中不产生重复记录 |
| **测试环境** | 开发环境 |

---

### TC-003：设备开关表 - 必填字段验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-003 |
| **测试标题** | 设备开关表必填字段缺失时插入失败 |
| **前置条件** | 设备开关表已创建，开关ID、开关序号为主键不可为空 |
| **测试步骤** | 1. 尝试插入开关ID为 NULL 的记录<br>2. 尝试插入开关序号为 NULL 的记录<br>3. 尝试插入开关标签为 NULL 的记录（如果该字段被定义为 NOT NULL） |
| **预期结果** | 所有 INSERT 操作均失败，返回 NOT NULL 约束错误 |
| **测试环境** | 开发环境 |

---

### TC-004：设备开关表 - 更新记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-004 |
| **测试标题** | 设备开关表正常更新非主键字段 |
| **前置条件** | 设备开关表中已存在记录：`开关ID='0xa4c1380bbd942cbe'`, `开关序号='state_l4'`, `开关标签='吊灯-01'` |
| **测试步骤** | 1. 执行 UPDATE，将开关标签改为'吊灯-01-改'，自动关灯开始时间改为'23:00'<br>2. SELECT 查询该记录 |
| **预期结果** | UPDATE 成功，查询结果显示开关标签和自动关灯开始时间已更新，开关ID和开关序号不变 |
| **测试环境** | 开发环境 |

---

### TC-005：设备开关表 - 删除记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-005 |
| **测试标题** | 设备开关表正常删除记录 |
| **前置条件** | 设备开关表中已存在待删除的记录 |
| **测试步骤** | 1. 记录删除前表中的总行数<br>2. 执行 DELETE 删除指定记录<br>3. SELECT 查询该记录<br>4. 统计删除后表中总行数 |
| **预期结果** | DELETE 成功，SELECT 查不到该记录，表总行数比删除前减少1 |
| **测试环境** | 开发环境 |

---

### TC-006：台桌设备表 - 正常创建记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-006 |
| **测试标题** | 台桌设备表正常插入一条完整记录 |
| **前置条件** | 数据库已创建 `台桌设备表`，表结构包含：**table_name_en（台桌名英文）、switch_index（开关序号）、switch_label（开关标签）** |
| **测试步骤** | 1. 执行 INSERT 插入记录：<br>`table_name_en='pool-table-01'`<br>`switch_index='state_l4'`<br>`switch_label='吊灯-01'`<br>2. SELECT 查询该记录 |
| **预期结果** | INSERT 成功，SELECT 能查到且字段值一致 |
| **测试环境** | 开发环境 |

---

### TC-007：台桌设备表 - 复合唯一约束

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-007 |
| **测试标题** | 台桌设备表复合唯一约束：相同台桌名英文+开关序号+开关标签不允许重复 |
| **前置条件** | 台桌设备表中已存在记录：`table_name_en='pool-table-01'`, `switch_index='state_l4'`, `switch_label='吊灯-01'` |
| **测试步骤** | 1. 尝试插入三个字段完全相同的另一条记录<br>2. 观察 INSERT 结果 |
| **预期结果** | INSERT 失败，返回唯一约束冲突错误 |
| **测试环境** | 开发环境 |

---

### TC-008：台桌设备表 - 多对多关系验证（一台桌对应多开关）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-008 |
| **测试标题** | 验证一台桌可关联多个开关设备 |
| **前置条件** | 台桌设备表为空 |
| **测试步骤** | 1. 插入记录A：`table_name_en='pool-table-01'`, `switch_index='state_l4'`, `switch_label='吊灯-01'`<br>2. 插入记录B：`table_name_en='pool-table-01'`, `switch_index='state_l5'`, `switch_label='吊灯-02'`<br>3. 查询台桌名为 'pool-table-01' 的所有记录 |
| **预期结果** | 两条记录均插入成功，查询返回2条记录，证明一台桌可对应多个开关 |
| **测试环境** | 开发环境 |

---

### TC-009：台桌设备表 - 多对多关系验证（一开关对应多台桌）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-009 |
| **测试标题** | 验证一开关设备可关联多个台桌 |
| **前置条件** | 台桌设备表为空 |
| **测试步骤** | 1. 插入记录A：`table_name_en='pool-table-01'`, `switch_index='state_l4'`, `switch_label='吊灯-01'`<br>2. 插入记录B：`table_name_en='pool-table-02'`, `switch_index='state_l4'`, `switch_label='吊灯-01'`<br>3. 查询开关标签为 '吊灯-01' 的所有记录 |
| **预期结果** | 两条记录均插入成功，查询返回2条记录，证明一开关可对应多个台桌 |
| **测试环境** | 开发环境 |

---

> ~~**TC-010：台桌设备表 - 更新记录**~~ **【已删除】** 需求为台桌设备关系提供 CRUD 操作，但根据审计S1，台桌设备表字段应为 (table_name_en, switch_index, switch_label)，不包含 switch_id。若业务上确实不需要更新台桌设备关系，则不应测试 Update。此处删除 Update 测试用例，如需保留请向需求方确认。

---

### TC-011：台桌设备表 - 删除记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-011 |
| **测试标题** | 台桌设备表正常删除记录 |
| **前置条件** | 台桌设备表中已存在待删除记录 |
| **测试步骤** | 1. 记录删除前行数<br>2. 执行 DELETE 删除指定记录<br>3. 查询确认该记录不存在 |
| **预期结果** | DELETE 成功，记录被删除，行数减少1 |
| **测试环境** | 开发环境 |

---

### TC-012：开关场景表 - 正常创建记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-012 |
| **测试标题** | 开关场景表正常插入一条记录 |
| **前置条件** | 数据库已创建 `开关场景表`，表结构包含：场景名称（主键）、动作（开灯/关灯）、开关数组（JSON） |
| **测试步骤** | 1. 执行 INSERT 插入记录：<br>`场景名称='全部开灯'`<br>`动作='开灯'`<br>`开关数组='[{"开关ID":"0xa4c1380bbd942cbe","开关序号":"state_l4"},{"开关ID":"0xa4c1380bbd942cbe","开关序号":"state_l5"}]'`<br>2. SELECT 查询该记录 |
| **预期结果** | INSERT 成功，查询结果中开关数组为合法 JSON 字符串，可被解析 |
| **测试环境** | 开发环境 |

---

### TC-013：开关场景表 - 场景名称唯一性约束

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-013 |
| **测试标题** | 开关场景表场景名称主键唯一性：不允许重复场景名称 |
| **前置条件** | 开关场景表中已存在记录：`场景名称='全部开灯'` |
| **测试步骤** | 1. 尝试插入另一条记录，场景名称同样为'全部开灯'，但动作或开关数组不同<br>2. 观察 INSERT 结果 |
| **预期结果** | INSERT 失败，返回主键/唯一约束冲突错误 |
| **测试环境** | 开发环境 |

---

### TC-014：开关场景表 - 开关数组JSON格式验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-014 |
| **测试标题** | 开关场景表开关数组字段必须为合法JSON格式 |
| **前置条件** | 开关场景表已创建 |
| **测试步骤** | 1. 尝试插入开关数组为非法JSON字符串的记录（如 `'not a json'` 或 `'[{"key":}]'`）<br>2. 观察 INSERT 结果 |
| **预期结果** | INSERT 失败或被应用层拦截，返回格式错误提示 |
| **测试环境** | 开发环境 |

---

### TC-015：开关场景表 - 删除记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-015 |
| **测试标题** | 开关场景表正常删除记录 |
| **前置条件** | 开关场景表中已存在待删除记录 |
| **测试步骤** | 1. 记录删除前行数<br>2. 执行 DELETE 删除指定场景<br>3. 查询确认不存在 |
| **预期结果** | DELETE 成功，记录被删除 |
| **测试环境** | 开发环境 |

---

### TC-016：开关场景表 - 动作字段合法性验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-016 |
| **测试标题** | 开关场景表动作字段只能是"开灯"或"关灯" |
| **前置条件** | 开关场景表已创建 |
| **测试步骤** | 1. 尝试插入动作='开灯'的记录 → 应成功<br>2. 尝试插入动作='关灯'的记录 → 应成功<br>3. 尝试插入动作='闪烁'或其他非法值的记录 → 应失败 |
| **预期结果** | 开灯/关灯插入成功，非法值插入被拒绝 |
| **测试环境** | 开发环境 |

---

> ~~**TC-017：开关场景表 - 更新记录**~~ **【已删除】** 根据需求文档第3节，开关场景表为 **CRD**（创建、读取、删除），**没有更新（U）功能**。根据审计S4，design.md 中多出的 `PUT /api/admin/switch-scenes/:id` 接口应删除，对应的 Update 测试用例也应删除。

---

## 二、后台admin管理页面测试

### TC-020：设备开关管理页面 - 列表展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-020 |
| **测试标题** | 设备开关管理页面正常展示列表数据 |
| **前置条件** | 1. 设备开关表已有至少3条测试数据<br>2. 使用管理员账号（tgadmin）登录后台admin系统 |
| **测试步骤** | 1. 登录后台admin<br>2. 在左侧菜单中找到并点击「设备开关管理」<br>3. 观察列表页面 |
| **预期结果** | 页面正常加载，列表中显示所有设备开关记录，包含开关ID、开关序号、开关标签、自动关灯开始时间、自动关灯结束时间各列 |
| **测试环境** | 开发环境 |

---

### TC-021：设备开关管理页面 - 新增设备开关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-021 |
| **测试标题** | 设备开关管理页面新增一条设备开关记录 |
| **前置条件** | 已登录后台admin，进入设备开关管理页面 |
| **测试步骤** | 1. 点击「新增」按钮<br>2. 填写表单：开关ID、开关序号、开关标签、自动关灯开始时间（如22:00）、自动关灯结束时间（如06:00）<br>3. 点击「保存」<br>4. 刷新列表查看 |
| **预期结果** | 保存成功，列表中出现新记录，数据库中多了一条记录 |
| **测试环境** | 开发环境 |

---

### TC-022：设备开关管理页面 - 编辑设备开关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-022 |
| **测试标题** | 设备开关管理页面编辑已有记录 |
| **前置条件** | 设备开关表中已有至少一条记录 |
| **测试步骤** | 1. 在列表中找到目标记录，点击「编辑」<br>2. 修改开关标签和自动关灯时间段<br>3. 点击「保存」<br>4. 刷新列表查看 |
| **预期结果** | 保存成功，列表中对应记录的字段值已更新 |
| **测试环境** | 开发环境 |

---

### TC-023：设备开关管理页面 - 删除设备开关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-023 |
| **测试标题** | 设备开关管理页面删除一条记录 |
| **前置条件** | 设备开关表中已有待删除的记录 |
| **测试步骤** | 1. 在列表中找到目标记录，点击「删除」<br>2. 确认删除弹窗<br>3. 刷新列表查看 |
| **预期结果** | 删除成功，该记录不再出现在列表中，数据库中对应记录被删除 |
| **测试环境** | 开发环境 |

---

### TC-024：台桌设备关系页面 - 列表展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-024 |
| **测试标题** | 台桌设备关系页面正常展示列表数据 |
| **前置条件** | 1. 台桌设备表已有至少3条测试数据<br>2. 已登录后台admin |
| **测试步骤** | 1. 在左侧菜单中点击「台桌设备关系」<br>2. 观察列表页面 |
| **预期结果** | 页面正常加载，列表中显示所有台桌设备关系记录，包含**台桌名英文（table_name_en）、开关序号（switch_index）、开关标签（switch_label）**各列 |
| **测试环境** | 开发环境 |

---

### TC-025：台桌设备关系页面 - 新增关系

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-025 |
| **测试标题** | 台桌设备关系页面新增一条台桌与开关的关联记录 |
| **前置条件** | 已登录后台admin，进入台桌设备关系页面 |
| **测试步骤** | 1. 点击「新增」按钮<br>2. 填写表单：**台桌名英文（如'pool-table-01'）、开关序号（如'state_l4'）、开关标签（如'吊灯-01'）**<br>3. 点击「保存」<br>4. 刷新列表 |
| **预期结果** | 保存成功，列表中出现新记录 |
| **测试环境** | 开发环境 |

---

> ~~**TC-026：台桌设备关系页面 - 编辑关系**~~ **【已删除】** 根据审计S1修正，台桌设备表字段为 (table_name_en, switch_index, switch_label)，UNIQUE 约束为 (table_name_en, switch_index, switch_label)。若业务需求中台桌设备关系不需要编辑功能，则删除此测试用例。如需保留编辑功能，请向需求方确认。

---

### TC-027：台桌设备关系页面 - 删除关系

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-027 |
| **测试标题** | 台桌设备关系页面删除一条记录 |
| **前置条件** | 台桌设备表中已有待删除的记录 |
| **测试步骤** | 1. 在列表中找到目标记录，点击「删除」<br>2. 确认删除<br>3. 刷新列表 |
| **预期结果** | 删除成功，该记录不再出现 |
| **测试环境** | 开发环境 |

---

### TC-028：开关场景表页面 - 列表展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-028 |
| **测试标题** | 开关场景表页面正常展示列表数据 |
| **前置条件** | 1. 开关场景表已有至少2条测试数据<br>2. 已登录后台admin |
| **测试步骤** | 1. 在左侧菜单中点击「开关场景表」<br>2. 观察列表页面 |
| **预期结果** | 页面正常加载，列表中显示所有场景记录，包含场景名称、动作、开关数组各列 |
| **测试环境** | 开发环境 |

---

### TC-029：开关场景表页面 - 新增场景（多行文本转JSON）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-029 |
| **测试标题** | 开关场景表新增时多行文本输入自动转换为JSON格式 |
| **前置条件** | 已登录后台admin，进入开关场景表页面 |
| **测试步骤** | 1. 点击「新增」按钮<br>2. 填写场景名称：'深夜模式'<br>3. 选择动作：'关灯'<br>4. 在开关数组多行文本输入框中，逐行输入：<br>&nbsp;&nbsp;&nbsp;&nbsp;`0xa4c1380bbd942cbe	state_l4`<br>&nbsp;&nbsp;&nbsp;&nbsp;`0xa4c1380bbd942cbe	state_l5`<br>&nbsp;&nbsp;&nbsp;&nbsp;（开关ID和开关序号之间用TAB键分割）<br>5. 点击「保存」<br>6. 查看数据库中该记录的开关数组字段值 |
| **预期结果** | 保存成功，数据库中开关数组字段存储为合法的JSON数组：<br>`[{"开关ID":"0xa4c1380bbd942cbe","开关序号":"state_l4"},{"开关ID":"0xa4c1380bbd942cbe","开关序号":"state_l5"}]` |
| **测试环境** | 开发环境 |

---

### TC-030：开关场景表页面 - 新增场景（多行文本格式错误处理）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-030 |
| **测试标题** | 开关场景表新增时多行文本格式错误应给出提示 |
| **前置条件** | 已登录后台admin，进入开关场景表页面 |
| **测试步骤** | 1. 点击「新增」<br>2. 填写场景名称和动作<br>3. 在多行文本框中输入格式错误的内容（如某行没有用TAB分割、或某行只写了开关ID没有写开关序号）<br>4. 点击「保存」 |
| **预期结果** | 系统应给出错误提示，告知用户格式不正确，不保存非法数据 |
| **测试环境** | 开发环境 |

---

### TC-031：开关场景表页面 - 删除场景

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-031 |
| **测试标题** | 开关场景表页面删除一条场景记录 |
| **前置条件** | 开关场景表中已有待删除的场景记录 |
| **测试步骤** | 1. 在列表中找到目标场景，点击「删除」<br>2. 确认删除<br>3. 刷新列表 |
| **预期结果** | 删除成功，该场景不再出现在列表中 |
| **测试环境** | 开发环境 |

---

> ~~**TC-031-U：开关场景表页面 - 编辑场景**~~ **【已删除】** 根据需求文档第3节，开关场景表为 CRD，没有更新功能。design.md 中的 PUT 接口应删除，对应的后台页面编辑功能也不应存在。

---

### TC-032：后台admin菜单 - 三个新菜单正确显示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-032 |
| **测试标题** | 后台admin左侧菜单中正确显示三个新增菜单项 |
| **前置条件** | 已登录后台admin系统 |
| **测试步骤** | 1. 查看左侧导航菜单<br>2. 检查是否存在「设备开关管理」「台桌设备关系」「开关场景表」三个菜单项 |
| **预期结果** | 三个菜单项均正确显示在导航菜单中，点击均可正常跳转 |
| **测试环境** | 开发环境 |

---

## 三、自动关灯功能测试

### TC-040：自动关灯 - 触发条件（更新≥40条台桌数据时触发）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-040 |
| **测试标题** | 台桌状态同步接口更新40条以上台桌数据时触发自动关灯 |
| **前置条件** | 1. 开发环境已部署自动关灯功能<br>2. 设备开关表、台桌设备表有测试数据<br>3. 当前时间处于某开关的自动关灯时间段内<br>4. 至少有41条台桌处于空闲状态且关联了自动关灯时段内的开关 |
| **测试步骤** | 1. 调用台桌状态同步接口，一次性更新41条台桌的状态为「空闲」<br>2. 检查系统日志<br>3. 检查是否有MQTT日志（测试环境只写日志不发送） |
| **预期结果** | 自动关灯功能被触发，日志中出现自动关灯执行的记录，符合关灯条件的开关被识别并处理 |
| **测试环境** | 开发环境 |

---

### TC-041：自动关灯 - 不触发条件（更新<40条台桌数据时不触发）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-041 |
| **测试标题** | 台桌状态同步接口更新少于40条台桌数据时不触发自动关灯 |
| **前置条件** | 1. 开发环境已部署自动关灯功能<br>2. 设备开关表、台桌设备表有测试数据 |
| **测试步骤** | 1. 调用台桌状态同步接口，一次性更新39条台桌的状态<br>2. 检查系统日志 |
| **预期结果** | 日志中不出现自动关灯执行的记录，自动关灯功能未被触发 |
| **测试环境** | 开发环境 |

---

### TC-042：自动关灯 - 边界值（恰好40条）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-042 |
| **测试标题** | 台桌状态同步接口更新恰好40条台桌数据时不触发（确认阈值是"40条以上"而非"40条及以上"） |
| **前置条件** | 1. 开发环境已部署自动关灯功能<br>2. 设备开关表、台桌设备表有测试数据 |
| **测试步骤** | 1. 调用台桌状态同步接口，一次性更新恰好40条台桌的状态<br>2. 检查系统日志 |
| **预期结果** | 根据需求描述"40条以上"，更新恰好40条时应**不触发**自动关灯。日志中不应出现自动关灯执行记录。<br>⚠️ 注：如需求实际意为"≥40条"，则此用例预期结果应为触发，需与需求方确认 |
| **测试环境** | 开发环境 |

---

### TC-043：自动关灯 - SQL查询：空闲+在时段=可能要关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-043 |
| **测试标题** | 验证"可能要关的灯"SQL查询正确性：空闲状态且当前时间在自动关灯时间段内 |
| **前置条件** | 1. 台桌表中有3条空闲台桌（如 pool-table-01/02/03）<br>2. 台桌设备表中这些台桌关联了开关A、B、C<br>3. 设备开关表中开关A、B、C的自动关灯时间段包含当前时间 |
| **测试步骤** | 1. 执行查询"可能要关的灯"的SQL逻辑<br>2. 检查查询结果 |
| **预期结果** | 查询结果包含开关A、B、C（以及它们关联的其他空闲台桌对应的开关），不包含不在自动关灯时间段内的开关 |
| **测试环境** | 开发环境 |

---

### TC-044：自动关灯 - SQL查询：非空闲+在时段=不能关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-044 |
| **测试标题** | 验证"不能关的灯"SQL查询正确性：非空闲状态但当前时间在自动关灯时间段内 |
| **前置条件** | 1. 台桌表中有2条非空闲台桌（使用中状态，如 pool-table-10/11）<br>2. 这些台桌关联了开关D、E<br>3. 设备开关表中开关D、E的自动关灯时间段包含当前时间 |
| **测试步骤** | 1. 执行查询"不能关的灯"的SQL逻辑<br>2. 检查查询结果 |
| **预期结果** | 查询结果包含开关D、E，因为它们关联的台桌不是空闲状态 |
| **测试环境** | 开发环境 |

---

### TC-045：自动关灯 - 差集计算正确性

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-045 |
| **测试标题** | 验证"确定要关的灯"=可能要关的灯-不能关的灯（差集计算） |
| **前置条件** | 1. 可能要关的灯集合 = {A, B, C, D}<br>2. 不能关的灯集合 = {C, D}（C、D关联的开关既有空闲台桌也有非空闲台桌） |
| **测试步骤** | 1. 执行自动关灯逻辑<br>2. 检查最终确定要关的灯集合 |
| **预期结果** | 确定要关的灯集合 = {A, B}，C和D被正确排除 |
| **测试环境** | 开发环境 |

---

### TC-046：自动关灯 - 差集计算：无交集情况

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-046 |
| **测试标题** | 验证差集计算：可能要关和不能关无交集时全部关灯 |
| **前置条件** | 1. 可能要关的灯集合 = {A, B, C}<br>2. 不能关的灯集合 = {}（所有关联台桌均为空闲） |
| **测试步骤** | 1. 执行自动关灯逻辑<br>2. 检查最终确定要关的灯集合 |
| **预期结果** | 确定要关的灯集合 = {A, B, C}，全部被选中关灯 |
| **测试环境** | 开发环境 |

---

### TC-047：自动关灯 - 差集计算：完全交集情况

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-047 |
| **测试标题** | 验证差集计算：可能要关和不能关完全相同时不关灯 |
| **前置条件** | 1. 可能要关的灯集合 = {A, B}<br>2. 不能关的灯集合 = {A, B}（所有关联台桌都有非空闲的） |
| **测试步骤** | 1. 执行自动关灯逻辑<br>2. 检查最终确定要关的灯集合 |
| **预期结果** | 确定要关的灯集合 = {}（空集），不发送任何关灯指令 |
| **测试环境** | 开发环境 |

---

### TC-048：自动关灯 - 测试环境MQTT只写日志不发送

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-048 |
| **测试标题** | 开发环境下自动关灯只写日志，不真实发送MQTT指令 |
| **前置条件** | 1. 开发环境（TGSERVICE_ENV=test）已部署<br>2. 触发条件满足（更新40条以上台桌数据）<br>3. 有确定要关的灯 |
| **测试步骤** | 1. 触发自动关灯功能<br>2. 检查系统日志中是否有MQTT相关日志<br>3. 使用 MQTT 客户端工具（如 MQTT Explorer）连接到 `mqtt://8.134.248.240:1883`，检查 Topic `tiangongguoji` 是否有新消息 |
| **预期结果** | 1. 日志中出现关灯指令的写入记录（包含开关ID和开关序号）<br>2. MQTT Broker 上**没有**收到新消息 |
| **测试环境** | 开发环境 |

---

### TC-049：自动关灯 - 生产环境MQTT真实发送

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-049 |
| **测试标题** | 生产环境下自动关灯真实发送MQTT指令 |
| **前置条件** | 1. 生产环境已部署（⚠️ 需用户确认后方可执行）<br>2. 触发条件满足<br>3. 有确定要关的灯 |
| **测试步骤** | 1. 触发自动关灯功能<br>2. 使用 MQTT 客户端工具连接到 `mqtt://8.134.248.240:1883`<br>3. 订阅 Topic `tiangongguoji`，等待消息<br>4. 检查接收到的消息内容 |
| **预期结果** | 1. MQTT Broker 收到消息<br>2. 消息 Topic 为 `tiangongguoji`<br>3. 消息格式为 `{"id":"$开关ID","$开关序号":"OFF"}` |
| **测试环境** | 生产环境 |

---

### TC-050：自动关灯 - MQTT关灯指令格式正确性

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-050 |
| **测试标题** | 验证MQTT关灯指令格式完全符合规范 |
| **前置条件** | 有确定要关的灯，开关ID=`0xa4c1380bbd942cbe`，开关序号=`state_l4` |
| **测试步骤** | 1. 触发自动关灯<br>2. 检查日志中或MQTT中发出的指令内容 |
| **预期结果** | 指令格式为：<br>`{"id":"0xa4c1380bbd942cbe","state_l4":"OFF"}`<br>其中 id 字段为开关ID的值，动态键为开关序号的值，值为"OFF" |
| **测试环境** | 开发环境 |

---

### TC-051：自动关灯 - MQTT开灯指令格式

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-051 |
| **测试标题** | 验证MQTT开灯指令格式正确（ON） |
| **前置条件** | 通过场景按钮或其他方式触发开灯操作 |
| **测试步骤** | 1. 触发开灯操作<br>2. 检查日志中或MQTT中发出的指令内容 |
| **预期结果** | 指令格式为：<br>`{"id":"$开关ID","$开关序号":"ON"}`<br>值为"ON"而非"OFF" |
| **测试环境** | 开发环境 |

---

### TC-052：自动关灯 - 自动关灯时间段跨午夜（含SQL Bug验证）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-052 |
| **测试标题** | 验证自动关灯时间段跨午夜（如22:00~06:00）的正确判断 |
| **前置条件** | 设备开关表中某开关的自动关灯时间段为 22:00~06:00 |
| **测试步骤** | 1. 在当前时间为 23:00 时执行自动关灯逻辑，检查该开关是否在"可能要关"集合中 → 应在<br>2. 在当前时间为 03:00 时执行自动关灯逻辑，检查该开关是否在"可能要关"集合中 → 应在<br>3. 在当前时间为 10:00 时执行自动关灯逻辑，检查该开关是否在"可能要关"集合中 → 不应在 |
| **预期结果** | 23:00和03:00时开关被判定在自动关灯时间段内，10:00时不在。<br><br>⚠️ **SQL Bug 验证**：如果使用 design.md 中的原始 SQL 逻辑 `TIME(?) >= TIME(auto_off_start) AND TIME(?) <= TIME(auto_off_end)`，则第1步（23:00）和第2步（03:00）会**错误判断为不在时段内**（BUG）。修复后的 SQL 应能正确处理跨午夜场景：`(start <= end AND ? >= start AND ? <= end) OR (start > end AND (? >= start OR ? <= end))`。本用例用于验证修复后的 SQL 是否正确。 |
| **测试环境** | 开发环境 |

---

### TC-053：自动关灯 - 自动关灯时间段不跨午夜

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-053 |
| **测试标题** | 验证自动关灯时间段不跨午夜（如14:00~18:00）的正确判断 |
| **前置条件** | 设备开关表中某开关的自动关灯时间段为 14:00~18:00 |
| **测试步骤** | 1. 在当前时间为 15:00 时执行 → 应在时间段内<br>2. 在当前时间为 12:00 时执行 → 不应在时间段内<br>3. 在当前时间为 20:00 时执行 → 不应在时间段内 |
| **预期结果** | 仅15:00时判定在时间段内 |
| **测试环境** | 开发环境 |

---

### TC-054：自动关灯 - 跨午夜时段判断（22:00~06:00）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-054 |
| **测试标题** | 跨午夜自动关灯时段（22:00~06:00）的全面时段判断测试 |
| **前置条件** | 设备开关表中某开关的自动关灯时间段为 22:00~06:00（跨午夜） |
| **测试步骤** | 分别以以下时间点执行自动关灯逻辑，检查该开关是否在"可能要关"集合中：<br>1. 21:59 → 不应在（时段前1分钟）<br>2. 22:00 → 应在（时段开始时刻）<br>3. 23:00 → 应在（时段内）<br>4. 00:00 → 应在（午夜时刻）<br>5. 03:00 → 应在（时段内）<br>6. 05:59 → 应在（时段内）<br>7. 06:00 → 应在（时段结束时刻）<br>8. 06:01 → 不应在（时段后1分钟）<br>9. 12:00 → 不应在（时段外） |
| **预期结果** | 22:00~06:00（含起止时刻）之间的所有时间点均应判定在时段内，其余时间判定在时段外 |
| **测试环境** | 开发环境 |

---

### TC-055：自动关灯 - 不跨午夜时段判断（08:00~22:00）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-055 |
| **测试标题** | 不跨午夜自动关灯时段（08:00~22:00）的全面时段判断测试 |
| **前置条件** | 设备开关表中某开关的自动关灯时间段为 08:00~22:00（不跨午夜） |
| **测试步骤** | 分别以以下时间点执行自动关灯逻辑，检查该开关是否在"可能要关"集合中：<br>1. 07:59 → 不应在（时段前1分钟）<br>2. 08:00 → 应在（时段开始时刻）<br>3. 12:00 → 应在（时段内）<br>4. 18:00 → 应在（时段内）<br>5. 21:59 → 应在（时段内）<br>6. 22:00 → 应在（时段结束时刻）<br>7. 22:01 → 不应在（时段后1分钟）<br>8. 03:00 → 不应在（时段外） |
| **预期结果** | 08:00~22:00（含起止时刻）之间的所有时间点均应判定在时段内，其余时间判定在时段外 |
| **测试环境** | 开发环境 |

---

### TC-056：自动关灯 - 边界值测试（恰好在start或end时间点）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-056 |
| **测试标题** | 自动关灯时间段边界值：恰好在开始或结束时间点的判断 |
| **前置条件** | 设备开关表中某开关的自动关灯时间段为 22:00~06:00 |
| **测试步骤** | 1. 在当前时间恰好为 22:00:00 时执行 → 应在时段内<br>2. 在当前时间恰好为 06:00:00 时执行 → 应在时段内<br>3. 在当前时间为 21:59:59 时执行 → 不应在时段内<br>4. 在当前时间为 06:00:01 时执行 → 不应在时段内<br>5. 再测试不跨午夜时段（08:00~22:00）：<br>&nbsp;&nbsp;a. 08:00:00 → 应在<br>&nbsp;&nbsp;b. 22:00:00 → 应在<br>&nbsp;&nbsp;c. 07:59:59 → 不应在<br>&nbsp;&nbsp;d. 22:00:01 → 不应在 |
| **预期结果** | 恰好等于start或end时间点时，应判定为在时段内（包含边界）；start前1秒和end后1秒判定为不在时段内 |
| **测试环境** | 开发环境 |

---

### TC-057：自动关灯 - 可能要关的灯集合为空

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-057 |
| **测试标题** | 空集合边界测试：所有台桌都非空闲时，"可能要关的灯"集合为空 |
| **前置条件** | 1. 开发环境已部署自动关灯功能<br>2. 设备开关表、台桌设备表有测试数据<br>3. 当前时间处于自动关灯时间段内<br>4. **所有台桌均为使用中状态**（无空闲台桌） |
| **测试步骤** | 1. 调用台桌状态同步接口，更新所有台桌状态为「使用中」<br>2. 检查自动关灯逻辑执行结果<br>3. 检查日志 |
| **预期结果** | 1. "可能要关的灯"集合为空<br>2. "确定要关的灯"集合也为空<br>3. 不发送任何关灯指令<br>4. 日志中记录"无需要关的灯"或类似信息 |
| **测试环境** | 开发环境 |

---

### TC-058：自动关灯 - 不能关的灯集合为空

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-058 |
| **测试标题** | 空集合边界测试：所有关联台桌均为空闲时，"不能关的灯"集合为空 |
| **前置条件** | 1. 开发环境已部署自动关灯功能<br>2. 设备开关表、台桌设备表有测试数据<br>3. 当前时间处于自动关灯时间段内<br>4. 所有台桌关联的开关对应的台桌**均为空闲状态** |
| **测试步骤** | 1. 触发自动关灯逻辑<br>2. 检查"不能关的灯"集合<br>3. 检查最终确定要关的灯集合 |
| **预期结果** | 1. "不能关的灯"集合为空<br>2. "确定要关的灯"集合 = "可能要关的灯"集合（全部关灯） |
| **测试环境** | 开发环境 |

---

### TC-059：自动关灯 - MQTT容错测试（MQTT服务器不可达）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-059 |
| **测试标题** | MQTT服务器不可达时的降级/容错处理 |
| **前置条件** | 1. 开发环境已部署自动关灯功能<br>2. 触发条件满足（更新40条以上台桌数据）<br>3. 有确定要关的灯<br>4. **模拟MQTT服务器不可达**（如关闭MQTT服务、修改配置为错误地址、或断开网络） |
| **测试步骤** | 1. 触发自动关灯功能<br>2. 检查系统日志<br>3. 观察系统是否崩溃或抛出未处理异常<br>4. 等待一段时间后恢复MQTT连接，检查系统是否自动恢复 |
| **预期结果** | 1. 系统不应崩溃或抛出未处理异常<br>2. 日志中应记录MQTT连接失败或发送失败的错误信息<br>3. 系统应继续正常运行（不影响其他功能）<br>4. 理想情况下：系统应具备重试机制或连接恢复后自动发送未发出的指令 |
| **测试环境** | 开发环境 |

---

## 四、定时自动开灯功能测试

> 以下测试用例针对 README 第1节需求中提到的"**定时自动开灯**"功能。该功能在 design.md 中完全缺失，需补充设计和实现。

### TC-060：定时自动开灯 - 触发条件（到达开灯时段时触发）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-060 |
| **测试标题** | 定时自动开灯：到达开灯时段时自动触发开灯 |
| **前置条件** | 1. 开发环境已部署定时自动开灯功能<br>2. 设备开关表中有开灯时段配置（如 06:00~22:00）<br>3. 当前时间为开灯时段开始前1分钟 |
| **测试步骤** | 1. 等待时间到达开灯时段开始时刻（如 06:00）<br>2. 检查系统日志<br>3. 检查是否发送MQTT开灯指令（测试环境只写日志） |
| **预期结果** | 1. 日志中出现定时自动开灯执行的记录<br>2. 对配置了开灯时段的开关发送ON指令<br>3. 开灯指令格式正确（见TC-051） |
| **测试环境** | 开发环境 |

---

### TC-061：定时自动开灯 - 非开灯时段不触发

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-061 |
| **测试标题** | 定时自动开灯：不在开灯时段内不应触发 |
| **前置条件** | 1. 开发环境已部署定时自动开灯功能<br>2. 设备开关表中开灯时段为 06:00~22:00<br>3. 当前时间为 23:00（非开灯时段） |
| **测试步骤** | 1. 触发定时自动开灯检查逻辑<br>2. 检查系统日志 |
| **预期结果** | 日志中不出现定时自动开灯执行记录，不发送任何开灯指令 |
| **测试环境** | 开发环境 |

---

### TC-062：定时自动开灯 - 开灯指令格式正确性

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-062 |
| **测试标题** | 验证定时自动开灯发送的MQTT指令格式为ON |
| **前置条件** | 1. 定时自动开灯功能已部署<br>2. 有开关配置了开灯时段 |
| **测试步骤** | 1. 触发定时自动开灯<br>2. 检查日志中发出的MQTT指令内容 |
| **预期结果** | 指令格式为：`{"id":"$开关ID","$开关序号":"ON"}`<br>值为"ON"（与关灯指令的"OFF"区分） |
| **测试环境** | 开发环境 |

---

### TC-063：定时自动开灯 - 跨午夜开灯时段判断（22:00~06:00）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-063 |
| **测试标题** | 定时自动开灯：跨午夜开灯时段（如22:00~06:00）的正确判断 |
| **前置条件** | 设备开关表中某开关的开灯时段为 22:00~06:00（跨午夜） |
| **测试步骤** | 分别以以下时间点检查该开关是否应触发自动开灯：<br>1. 21:59 → 不应触发<br>2. 22:00 → 应触发<br>3. 00:00 → 应触发<br>4. 03:00 → 应触发<br>5. 06:00 → 应触发<br>6. 06:01 → 不应触发<br>7. 12:00 → 不应触发 |
| **预期结果** | 22:00~06:00（含起止时刻）之间的所有时间点均应触发自动开灯，其余时间不触发 |
| **测试环境** | 开发环境 |

---

### TC-064：定时自动开灯 - 不跨午夜开灯时段判断（08:00~22:00）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-064 |
| **测试标题** | 定时自动开灯：不跨午夜开灯时段（如08:00~22:00）的正确判断 |
| **前置条件** | 设备开关表中某开关的开灯时段为 08:00~22:00（不跨午夜） |
| **测试步骤** | 分别以以下时间点检查该开关是否应触发自动开灯：<br>1. 07:59 → 不应触发<br>2. 08:00 → 应触发<br>3. 12:00 → 应触发<br>4. 18:00 → 应触发<br>5. 22:00 → 应触发<br>6. 22:01 → 不应触发<br>7. 03:00 → 不应触发 |
| **预期结果** | 08:00~22:00（含起止时刻）之间的所有时间点均应触发自动开灯，其余时间不触发 |
| **测试环境** | 开发环境 |

---

### TC-065：定时自动开灯 - 测试环境MQTT只写日志不发送

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-065 |
| **测试标题** | 开发环境下定自动开灯只写日志，不真实发送MQTT指令 |
| **前置条件** | 1. 开发环境（TGSERVICE_ENV=test）已部署<br>2. 定时自动开灯触发条件满足 |
| **测试步骤** | 1. 触发定时自动开灯功能<br>2. 检查系统日志<br>3. 使用 MQTT 客户端工具检查 Broker 上是否有新消息 |
| **预期结果** | 1. 日志中出现开灯指令的写入记录<br>2. MQTT Broker 上**没有**收到新消息 |
| **测试环境** | 开发环境 |

---

### TC-066：定时自动开灯 - 自动开灯延迟时间配置测试

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-066 |
| **测试标题** | 验证定时自动开灯支持延迟时间配置 |
| **前置条件** | 1. 开发环境已部署定时自动开灯功能<br>2. 配置了开灯延迟时间（如延迟5分钟） |
| **测试步骤** | 1. 到达开灯时段开始时刻<br>2. 检查是否在延迟时间后才发送开灯指令<br>3. 修改延迟时间为0，检查是否立即发送 |
| **预期结果** | 1. 设置了延迟时间时，开灯指令在延迟后才发出<br>2. 延迟时间为0时，开灯指令立即发出<br>3. 延迟时间配置可从配置文件或数据库读取 |
| **测试环境** | 开发环境 |

---

## 五、前台H5智能开关页面测试

### TC-070：智能开关页面 - 权限控制（店长可访问）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-070 |
| **测试标题** | 店长角色用户可访问智能开关页面 |
| **前置条件** | 1. 开发环境H5已部署<br>2. 存在一个店长角色的测试账号 |
| **测试步骤** | 1. 使用店长账号登录H5<br>2. 进入管理功能<br>3. 查找「智能开关」按钮/入口<br>4. 点击进入智能开关页面 |
| **预期结果** | 店长可以看到智能开关入口，点击进入后页面正常展示 |
| **测试环境** | 开发环境 |

---

### TC-071：智能开关页面 - 权限控制（助教管理可访问）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-071 |
| **测试标题** | 助教管理角色用户可访问智能开关页面 |
| **前置条件** | 1. 开发环境H5已部署<br>2. 存在一个助教管理角色的测试账号 |
| **测试步骤** | 1. 使用助教管理账号登录H5<br>2. 进入管理功能<br>3. 查找「智能开关」按钮/入口<br>4. 点击进入智能开关页面 |
| **预期结果** | 助教管理可以看到智能开关入口，页面正常展示 |
| **测试环境** | 开发环境 |

---

### TC-072：智能开关页面 - 权限控制（普通用户不可访问）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-072 |
| **测试标题** | 非店长/非助教管理角色用户不可访问智能开关页面 |
| **前置条件** | 1. 开发环境H5已部署<br>2. 存在一个普通用户角色的测试账号 |
| **测试步骤** | 1. 使用普通用户账号登录H5<br>2. 进入管理功能<br>3. 检查是否能看到「智能开关」按钮/入口 |
| **预期结果** | 普通用户看不到智能开关入口，或点击后提示无权限 |
| **测试环境** | 开发环境 |

---

### TC-073：智能开关页面 - 页面UI展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-073 |
| **测试标题** | 智能开关页面UI设计精美、有科技感，与H5其他管理页面风格一致 |
| **前置条件** | 1. 有权限的用户已登录H5<br>2. 进入智能开关页面 |
| **测试步骤** | 1. 观察页面整体布局、配色、字体<br>2. 检查是否有科技感的设计元素（如渐变色、卡片布局、图标等）<br>3. 与H5其他管理页面（如台桌管理、订单管理等）对比风格 |
| **预期结果** | 1. 页面布局清晰，元素排列合理<br>2. 具有科技感设计（不要求具体样式，但需有视觉特色）<br>3. 与H5其他管理页面的整体风格差异不大（色调、字体、导航风格一致） |
| **测试环境** | 开发环境 |

---

### TC-074：智能开关页面 - 自动关灯启停按钮（启动）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-074 |
| **测试标题** | 智能开关页面「自动关灯功能」启动按钮正常工作 |
| **前置条件** | 1. 有权限用户已登录H5<br>2. 进入智能开关页面<br>3. 自动关灯功能当前为暂停状态 |
| **测试步骤** | 1. 点击「启动」按钮（或开关切换为开启）<br>2. 观察按钮状态变化<br>3. 触发台桌状态同步接口（更新40条以上），观察是否执行自动关灯 |
| **预期结果** | 1. 按钮状态变为"已启动"（或开关显示为开启状态）<br>2. 后续触发台桌同步时自动关灯功能正常执行 |
| **测试环境** | 开发环境 |

---

### TC-075：智能开关页面 - 自动关灯启停按钮（暂停）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-075 |
| **测试标题** | 智能开关页面「自动关灯功能」暂停按钮正常工作 |
| **前置条件** | 1. 有权限用户已登录H5<br>2. 进入智能开关页面<br>3. 自动关灯功能当前为启动状态 |
| **测试步骤** | 1. 点击「暂停」按钮（或开关切换为关闭）<br>2. 观察按钮状态变化<br>3. 触发台桌状态同步接口（更新40条以上），观察是否执行自动关灯 |
| **预期结果** | 1. 按钮状态变为"已暂停"（或开关显示为关闭状态）<br>2. 后续触发台桌同步时自动关灯功能**不执行** |
| **测试环境** | 开发环境 |

---

### TC-076：智能开关页面 - 场景按钮展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-076 |
| **测试标题** | 智能开关页面根据开关场景表动态生成场景按钮 |
| **前置条件** | 1. 开关场景表中已有3条场景记录：'全部开灯'(开灯)、'深夜模式'(关灯)、'清洁模式'(关灯)<br>2. 有权限用户已进入智能开关页面 |
| **测试步骤** | 1. 观察页面上的场景按钮区域 |
| **预期结果** | 页面上显示3个场景按钮，按钮名称分别为'全部开灯'、'深夜模式'、'清洁模式' |
| **测试环境** | 开发环境 |

---

### TC-077：智能开关页面 - 场景按钮点击（开灯）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-077 |
| **测试标题** | 点击开灯场景按钮，对开关数组中的所有开关发送开灯指令 |
| **前置条件** | 1. 开关场景表中存在场景'全部开灯'，动作为'开灯'，开关数组包含2个开关<br>2. 有权限用户已进入智能开关页面 |
| **测试步骤** | 1. 点击「全部开灯」场景按钮<br>2. 检查日志中MQTT指令记录 |
| **预期结果** | 1. 对该场景开关数组中的每个开关发送ON指令<br>2. 日志中出现对应的开灯记录 |
| **测试环境** | 开发环境 |

---

### TC-078：智能开关页面 - 场景按钮点击（关灯）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-078 |
| **测试标题** | 点击关灯场景按钮，对开关数组中的所有开关发送关灯指令 |
| **前置条件** | 1. 开关场景表中存在场景'深夜模式'，动作为'关灯'，开关数组包含2个开关<br>2. 有权限用户已进入智能开关页面 |
| **测试步骤** | 1. 点击「深夜模式」场景按钮<br>2. 检查日志中MQTT指令记录 |
| **预期结果** | 1. 对该场景开关数组中的每个开关发送OFF指令<br>2. 日志中出现对应的关灯记录 |
| **测试环境** | 开发环境 |

---

### TC-079：智能开关页面 - 开关标签下拉框展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-079 |
| **测试标题** | 智能开关页面开关标签下拉框正确展示所有开关标签 |
| **前置条件** | 1. 设备开关表中存在多个不同的开关标签（如'吊灯-01'、'吊灯-02'、'吊灯-03'）<br>2. 有权限用户已进入智能开关页面 |
| **测试步骤** | 1. 找到开关标签下拉框<br>2. 点击展开下拉选项 |
| **预期结果** | 下拉框中列出设备开关表中所有不重复的开关标签 |
| **测试环境** | 开发环境 |

---

### TC-080：智能开关页面 - 开关标签选择后开灯

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-080 |
| **测试标题** | 选择开关标签后点击开灯，对该标签下所有开关发送开灯指令 |
| **前置条件** | 1. 设备开关表中开关标签'吊灯-01'对应3个开关（不同开关ID或开关序号）<br>2. 有权限用户已进入智能开关页面 |
| **测试步骤** | 1. 在下拉框中选择'吊灯-01'<br>2. 点击「开灯」按钮<br>3. 检查日志中MQTT指令记录 |
| **预期结果** | 对'吊灯-01'标签下的所有3个开关分别发送ON指令，日志中出现3条开灯记录 |
| **测试环境** | 开发环境 |

---

### TC-081：智能开关页面 - 开关标签选择后关灯

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-081 |
| **测试标题** | 选择开关标签后点击关灯，对该标签下所有开关发送关灯指令 |
| **前置条件** | 1. 设备开关表中开关标签'吊灯-02'对应2个开关<br>2. 有权限用户已进入智能开关页面 |
| **测试步骤** | 1. 在下拉框中选择'吊灯-02'<br>2. 点击「关灯」按钮<br>3. 检查日志中MQTT指令记录 |
| **预期结果** | 对'吊灯-02'标签下的所有2个开关分别发送OFF指令，日志中出现2条关灯记录 |
| **测试环境** | 开发环境 |

---

### TC-082：智能开关页面 - 开关标签未选择时操作

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-082 |
| **测试标题** | 未选择开关标签时点击开灯/关灯应有提示 |
| **前置条件** | 有权限用户已进入智能开关页面 |
| **测试步骤** | 1. 不选择任何开关标签<br>2. 直接点击「开灯」或「关灯」按钮 |
| **预期结果** | 系统给出提示信息（如"请先选择开关标签"），不发送任何MQTT指令 |
| **测试环境** | 开发环境 |

---

## 六、数据导入测试

### TC-085：Excel导入 - 设备开关表数据导入

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-085 |
| **测试标题** | 从 SwitchManagement.xlsx 的【设备开关表】页面导入数据到设备开关表 |
| **前置条件** | 1. 开发环境已部署数据导入功能<br>2. Excel文件 `/TG/docs/智能开关改造/SwitchManagement.xlsx` 存在<br>3. 设备开关表为空 |
| **测试步骤** | 1. 执行数据导入功能，读取【设备开关表】页面数据<br>2. 统计导入后的记录数<br>3. 随机抽取3条记录，与Excel中对应行对比字段值 |
| **预期结果** | 1. 导入成功无报错<br>2. 设备开关表记录数与Excel数据行数一致<br>3. 抽样对比字段值完全匹配（开关ID、开关序号、开关标签、自动关灯时间段） |
| **测试环境** | 开发环境 |

---

### TC-086：Excel导入 - 设备开关表数据导入完整性验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-086 |
| **测试标题** | 验证导入的设备开关表数据完整性（所有字段均有值） |
| **前置条件** | 已完成TC-085的数据导入 |
| **测试步骤** | 1. 查询设备开关表中所有记录<br>2. 检查每条记录的开关ID、开关序号、开关标签、自动关灯开始时间、自动关灯结束时间是否均非空 |
| **预期结果** | 所有记录的所有必填字段均有值，无NULL或空字符串 |
| **测试环境** | 开发环境 |

---

### TC-087：Excel导入 - 台桌设备多对多关系数据导入

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-087 |
| **测试标题** | 从 Excel【设备开关表】页面最后一列"关联台桌"导入台桌设备关系数据 |
| **前置条件** | 1. Excel文件存在且最后一列包含关联台桌英文名<br>2. 台桌设备表为空<br>3. 已确认一个开关设备可能关联多个台桌（多对多） |
| **测试步骤** | 1. 执行数据导入，解析最后一列的关联台桌数据<br>2. 如果一个开关关联多个台桌，检查是否在台桌设备表中生成了多条记录<br>3. 统计导入后的记录总数 |
| **预期结果** | 1. 导入成功<br>2. 多对多关系正确展开：如果一个开关关联N个台桌，则台桌设备表中生成N条记录<br>3. 每条记录的**台桌名英文（table_name_en）、开关序号（switch_index）、开关标签（switch_label）**均正确 |
| **测试环境** | 开发环境 |

---

### TC-088：Excel导入 - 多对多关系验证（反向查询）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-088 |
| **测试标题** | 验证导入后的多对多关系：从台桌视角和开关视角双向查询正确 |
| **前置条件** | 已完成TC-087的数据导入 |
| **测试步骤** | 1. 选取Excel中一个关联了多个台桌的开关（如开关标签'吊灯-01'关联了 pool-table-01, pool-table-02, pool-table-03）<br>2. 查询台桌设备表中该开关标签的所有记录<br>3. 选取Excel中一个被多个开关关联的台桌<br>4. 查询台桌设备表中该台桌的所有记录 |
| **预期结果** | 1. 查询结果与Excel中的关联关系完全一致<br>2. 台桌→开关和开关→台桌的双向查询均正确 |
| **测试环境** | 开发环境 |

---

### TC-089：Excel导入 - 重复导入处理

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-089 |
| **测试标题** | 重复执行Excel导入时不会产生重复数据 |
| **前置条件** | 已完成一次Excel数据导入，设备开关表和台桌设备表已有数据 |
| **测试步骤** | 1. 记录当前设备开关表和台桌设备表的记录数<br>2. 再次执行相同的数据导入<br>3. 再次统计两张表的记录数 |
| **预期结果** | 第二次导入后记录数不变（或导入逻辑明确处理了重复数据，如先清空再导入，或跳过已存在的记录） |
| **测试环境** | 开发环境 |

---

## 七、集成/综合测试

### TC-090：集成测试 - 完整自动关灯流程

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-090 |
| **测试标题** | 从台桌状态变更到自动关灯的完整流程端到端测试 |
| **前置条件** | 1. 所有数据库表已导入初始数据<br>2. 开发环境已部署全部功能<br>3. 自动关灯功能处于启动状态 |
| **测试步骤** | 1. 确认当前时间处于某些开关的自动关灯时间段内<br>2. 通过台桌状态同步接口更新50条台桌数据，其中40条设为空闲、10条设为使用中<br>3. 检查日志中的自动关灯执行记录<br>4. 检查MQTT日志（测试环境只写日志） |
| **预期结果** | 1. 自动关灯功能被触发（更新50条 > 40条）<br>2. "可能要关的灯"包含40条空闲台桌关联的所有开关<br>3. "不能关的灯"包含10条使用中台桌关联的开关<br>4. "确定要关的灯"为差集结果<br>5. 对差集中的每个开关发送OFF指令，指令格式正确 |
| **测试环境** | 开发环境 |

---

### TC-091：集成测试 - 场景按钮端到端

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-091 |
| **测试标题** | 从后台新增场景到前台点击场景按钮的完整流程 |
| **前置条件** | 1. 所有数据库表已有数据<br>2. 开发环境已部署 |
| **测试步骤** | 1. 登录后台admin，进入开关场景表页面<br>2. 新增一个场景'测试场景'，动作'关灯'，多行文本输入2个开关<br>3. 保存后检查数据库中开关数组字段为JSON格式<br>4. 登录H5前台，进入智能开关页面<br>5. 点击「测试场景」按钮<br>6. 检查日志 |
| **预期结果** | 1. 后台新增成功，开关数组为JSON格式<br>2. 前台页面中出现'测试场景'按钮<br>3. 点击后对2个开关发送OFF指令，日志中记录正确 |
| **测试环境** | 开发环境 |

---

### TC-092：集成测试 - 时间类使用验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-092 |
| **测试标题** | 验证所有时间处理均使用了现有的时间类（TimeUtil） |
| **前置条件** | 开发环境代码已部署 |
| **测试步骤** | 1. 检查自动关灯相关代码中获取当前时间的方式<br>2. 检查是否使用了 `datetime('now')` 或 `datetime('now', 'localtime')` 等禁止的方式<br>3. 检查是否使用了 TimeUtil.nowDB() / TimeUtil.todayStr() 等统一工具类 |
| **预期结果** | 所有时间处理均使用了 TimeUtil 工具类，未使用禁止的SQL datetime()函数或手动时区偏移 |
| **测试环境** | 开发环境 |

---

### TC-093：集成测试 - 数据库连接验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-093 |
| **测试标题** | 验证所有数据库操作均使用了 db/index.js 中的唯一连接 |
| **前置条件** | 开发环境代码已部署 |
| **测试步骤** | 1. 检查智能开关相关代码中是否有 `new sqlite3.Database(path)` 等创建新连接的代码<br>2. 检查是否使用了 `require('./db/index')` 中的 db/dbRun/dbAll/dbGet |
| **预期结果** | 所有数据库操作均使用 db/index.js 中的现有连接，未创建新的数据库连接 |
| **测试环境** | 开发环境 |

---

### TC-094：集成测试 - writequeue队列验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-094 |
| **测试标题** | 验证所有数据库写入操作均使用了writequeue队列 |
| **前置条件** | 开发环境代码已部署 |
| **测试步骤** | 1. 检查智能开关相关的所有写入操作（INSERT、UPDATE、DELETE）<br>2. 确认是否均通过writequeue队列执行 |
| **预期结果** | 所有数据库写入操作均通过writequeue队列执行，未直接调用 db.run() 等 |
| **测试环境** | 开发环境 |

---

### TC-095：集成测试 - MQTT配置从配置文件读取

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-095 |
| **测试标题** | 验证MQTT配置（Host/Port/Username/Password）从.config配置文件读取 |
| **前置条件** | 开发环境已部署，.config文件中已写入MQTT配置 |
| **测试步骤** | 1. 检查代码中MQTT配置是否硬编码<br>2. 确认是否从配置文件读取 Host、Port、Username、Password |
| **预期结果** | MQTT配置全部从配置文件读取，代码中无硬编码的密码或连接地址 |
| **测试环境** | 开发环境 |

---

### TC-096：集成测试 - 定时自动开灯完整流程

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-096 |
| **测试标题** | 从定时任务触到MQTT发送开灯指令的完整流程端到端测试 |
| **前置条件** | 1. 所有数据库表已导入初始数据<br>2. 开发环境已部署定时自动开灯功能<br>3. 有开关配置了开灯时段 |
| **测试步骤** | 1. 等待或手动触发定时自动开灯检查<br>2. 检查当前时间是否处于某开关的开灯时段内<br>3. 检查日志中的定时自动开灯执行记录<br>4. 检查MQTT日志（测试环境只写日志） |
| **预期结果** | 1. 定时自动开灯功能被触发<br>2. 处于开灯时段内的开关被识别<br>3. 对每个应开灯的开关发送ON指令，指令格式正确 |
| **测试环境** | 开发环境 |

---

### TC-097：集成测试 - 自动关灯与自动开灯互不干扰

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-097 |
| **测试标题** | 验证自动关灯和定时自动开灯功能互不干扰 |
| **前置条件** | 1. 自动关灯和定时自动开灯功能均已部署<br>2. 自动关灯和自动开灯配置了不同的时间段 |
| **测试步骤** | 1. 在自动关灯时段内触发台桌状态同步 → 应执行自动关灯<br>2. 在自动开灯时段内触发定时检查 → 应执行自动开灯<br>3. 在两个时段重叠时（如都配置了22:00） → 检查行为是否符合预期 |
| **预期结果** | 1. 自动关灯和自动开灯各自独立触发，互不影响<br>2. 重叠时段的行为符合设计预期（需向需求方确认优先级） |
| **测试环境** | 开发环境 |

---

### TC-098：集成测试 - 台桌设备表字段正确性验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-098 |
| **测试标题** | 验证台桌设备表字段为 table_name_en, switch_index, switch_label |
| **前置条件** | 开发环境代码已部署 |
| **测试步骤** | 1. 检查数据库表结构，确认字段名为 table_name_en, switch_index, switch_label<br>2. 确认不存在 switch_id 字段<br>3. 确认 UNIQUE 约束为 (table_name_en, switch_index, switch_label) |
| **预期结果** | 1. 表字段正确为 table_name_en, switch_index, switch_label<br>2. 不存在 switch_id 字段<br>3. UNIQUE 约束正确为 (table_name_en, switch_index, switch_label) |
| **测试环境** | 开发环境 |

---

### TC-099：集成测试 - 开关场景表无Update接口验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-099 |
| **测试标题** | 验证开关场景表没有Update API接口 |
| **前置条件** | 开发环境代码已部署 |
| **测试步骤** | 1. 检查代码中是否存在 `PUT /api/admin/switch-scenes/:id` 或类似更新接口<br>2. 尝试调用更新接口（如存在）<br>3. 检查后台admin页面是否有编辑场景的功能 |
| **预期结果** | 1. 代码中不存在开关场景表的更新API接口<br>2. 后台admin页面不提供编辑场景的功能<br>3. 开关场景表仅支持创建（C）、读取（R）、删除（D） |
| **测试环境** | 开发环境 |

---

## 测试用例统计

| 模块 | 用例数 | 用例编号范围 |
|------|--------|-------------|
| 数据库表测试 | 14（删除2个Update） | TC-001 ~ TC-016（不含TC-010, TC-017） |
| 后台admin管理页面测试 | 11（删除1个Edit） | TC-020 ~ TC-032（不含TC-026） |
| 自动关灯功能测试 | 20 | TC-040 ~ TC-059 |
| 定时自动开灯功能测试 | 7 | TC-060 ~ TC-066 |
| 前台H5智能开关页面测试 | 12 | TC-070 ~ TC-082 |
| 数据导入测试 | 5 | TC-085 ~ TC-089 |
| 集成/综合测试 | 10 | TC-090 ~ TC-099 |
| **合计** | **79** | TC-001 ~ TC-099（部分编号已删除） |

---

## 变更说明（v1.0 → v1.1）

### 删除的测试用例
| 编号 | 原因 | 依据 |
|------|------|------|
| ~~TC-010~~ | 台桌设备表字段修正后不需要Update测试 | 审计S1 |
| ~~TC-017~~ | 开关场景表为CRD，无Update功能 | 审计S4 |
| ~~TC-026~~ | 台桌设备关系页面编辑功能移除 | 审计S1 |

### 新增的测试用例
| 编号 | 标题 | 依据 |
|------|------|------|
| TC-054 | 跨午夜时段判断（22:00~06:00） | 审计-跨午夜边界增强 |
| TC-055 | 不跨午夜时段判断（08:00~22:00） | 审计-跨午夜边界增强 |
| TC-056 | 边界值测试（恰好在start或end时间点） | 审计-跨午夜边界增强 |
| TC-057 | 可能要关的灯集合为空 | 审计-空集合边界 |
| TC-058 | 不能关的灯集合为空 | 审计-空集合边界 |
| TC-059 | MQTT容错测试（MQTT服务器不可达） | 审计-MQTT容错 |
| TC-060~TC-066 | 定时自动开灯功能测试（7个用例） | 审计S3 |
| TC-096 | 定时自动开灯完整流程端到端 | 审计S3 |
| TC-097 | 自动关灯与自动开灯互不干扰 | 补充 |
| TC-098 | 台桌设备表字段正确性验证 | 审计S1 |
| TC-099 | 开关场景表无Update接口验证 | 审计S4 |

### 修改的测试用例
| 编号 | 修改内容 |
|------|----------|
| TC-006~TC-009 | 字段名从"台桌名英文/开关序号/开关标签"改为 `table_name_en / switch_index / switch_label` |
| TC-011 | 字段名同步修正 |
| TC-024~TC-025 | 字段名同步修正 |
| TC-027 | 字段名同步修正 |
| TC-052 | 补充SQL Bug验证说明，指出原始SQL在跨午夜场景下的判断缺陷 |
| TC-087 | 字段名同步修正 |

---

## 注意事项

1. **生产环境测试**（TC-049）必须在用户明确确认后方可执行
2. **TC-042** 的边界值预期结果需与需求方确认"40条以上"是否包含40条
3. 所有MQTT相关测试在开发环境下均只验证日志，不验证真实消息发送
4. 数据库相关测试使用开发环境数据库，不影响生产数据
5. **定时自动开灯功能**（TC-060~TC-066）对应的设计方案尚缺失，需先补充design.md后方可编码实现
6. **开关场景表Update相关**：TC-017（数据库Update测试）和TC-026（后台页面Edit）已删除，后台页面不应提供编辑功能
