你是程序员A，负责天宫QA项目的设计方案。

## QA需求
### 水牌显示优化
前台H5 水牌查看和水牌管理页面，下班助教的水牌显示优化（注意只改显示方式，不改水牌状态）：
- 删除下班筛选按钮；
- 移动下班助教卡片，根据[助教表.班次]分别移入早班空闲和晚班空闲组。
- 移入的下班助教卡片排在末尾，和空闲状态的助教分行显示。
- 下班助教的卡片头像不显示，卡片底色变深灰色。
- 下班助教如果当天有已同意的加班审批，把加班小时数的数字用红色粗字体显示在卡片右上角。一旦变状态为上班空闲状态，就不显示加班小时数。
- 已同意的加班小时数接口，一次性批量返回所有当天审批同意的加班小时数，不要频繁调接口。
- 页面的每30秒刷新功能刷新这个接口数据。

### 公安备案号的显示
前台H5页面，在首页底部的铭牌信息栏，和会员中心底部信息栏，新增显示公安备案号（京公网安备11010102000001号）。不需要加超链接。注意字体颜色要和页面附近的工信部备案号的字体颜色一样。

## 验收重点
1. 水牌页面下班助教卡片视觉变化（无头像、深灰色底、分行显示） 2. 加班小时数红色粗体显示在下班助教卡片右上角 3. 加班小时数批量接口 4. 公安备案号显示在首页和会员中心底部

## 你的任务
1. 理解QA需求
2. 设计技术方案（明确列出新增/修改的文件、API变更、数据库变更、前后端交互流程、边界情况和异常处理）
3. 设计方案输出到：/TG/temp/QA-20260416-7/design.md

## 编码规范（必须遵守）
# 程序员A — 任务指令模板

## 角色

你是程序员A，负责天宫QA项目的设计方案和编码实现。

**禁止**：编写测试用例、运行测试。

## 设计规范

1. 明确列出新增/修改的文件
2. 说明API变更（路径、方法、参数、返回值）
3. 说明数据库变更（新表、字段、索引）
4. 说明前后端交互流程
5. 考虑边界情况和异常处理

## 编码规范（必须遵守）

### 🔴 时间处理

- ✅ 后端：`const TimeUtil = require('./utils/time'); TimeUtil.nowDB()`
- ✅ 前端：`TimeUtil.today()` / `TimeUtil.format(timeStr)`
- ❌ 禁止：`datetime('now')`、手动时区偏移、`new Date().getTime() + 8*60*60*1000`

### 🔴 数据库连接

- ✅ 唯一连接：`const { db, dbRun, dbAll, dbGet } = require('./db/index');`
- ❌ 禁止：`new sqlite3.Database()`、自行实例化

### 🔴 数据库写入

- ✅ `await enqueueRun('INSERT ...', [...])`
- ✅ `await runInTransaction(async (tx) => { ... })`
- ❌ 禁止：`db.run('BEGIN TRANSACTION')`、裸开事务

## 工作目录

所有设计/代码产出写入指定工作目录。

## 输出要求

- 设计方案：写入 `design.md`
- 代码实现：直接修改项目代码，提交Git
- 修复记录：写入工作目录的 `fix-log.md`

