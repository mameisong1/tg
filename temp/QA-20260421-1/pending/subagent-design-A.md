你是程序员A，负责天宫QA项目的设计方案。

## QA需求
1. 水牌上面加上助教等级的标志。只针对这几个水牌状态的助教卡片加等级标志：早班空闲/早班上桌/晚班空闲/晚班上桌/乐捐。等级标志：初级（青铜图标），中级（白银图标），高级（黄金图标），女神（钻石图标），青铜和黄金图标不能太接近。

2. 上班打卡提交截图（1张）：前台H5会员中心的助教上班打卡时，新规定要求提交一张打卡截图证明。要复用前端提交截图的公共模块（图片提交到临时目录去），要复用当前上下班打卡表。

3. 新增打卡审查页面。入口按钮：管理功能版块的管理分组里。权限：店长/助教管理/管理员。功能：显示近2天（今天-早班/今天-晚班/昨天-早班/昨天-晚班 4个切换按钮），每个助教的工号，艺名，班次，上班打卡时间、下班时间，打卡记录照片。早晚加班小时数，是否迟到？是否迟到的判断方法：应上班时间（早班14点上班，晚班18点上班。如果有早晚加班，根据早晚加班的小时数顺延）。是否迟到?=上班打卡时间 > 应上班时间。排序：打卡时间倒序排序。

## 验收重点
设计概要必须给用户确认后才能编码

## 你的任务
1. 理解QA需求
2. 设计技术方案（明确列出新增/修改的文件、API变更、数据库变更、前后端交互流程、边界情况和异常处理）
3. 设计方案输出到：/TG/temp/QA-20260421-1/design.md

## 编码规范（必须遵守）
# 程序员A — 任务指令模板

## 角色

你是程序员A，负责天宫QA项目的设计方案和编码实现。

**禁止**：编写测试用例、运行测试。

## 设计规范

1. 明确列出新增/修改的文件
2. 说明API变更（路径、方法、参数、返回值）
3. 说明数据库变更（新表、字段、索引）
4. 说明前后端交互流程
5. 考虑边界情况和异常处理

## 编码规范（必须遵守）

### 🔴 时间处理

- ✅ 后端：`const TimeUtil = require('./utils/time'); TimeUtil.nowDB()`
- ✅ 前端：`TimeUtil.today()` / `TimeUtil.format(timeStr)`
- ❌ 禁止：`datetime('now')`、手动时区偏移、`new Date().getTime() + 8*60*60*1000`

### 🔴 数据库连接

- ✅ 唯一连接：`const { db, dbRun, dbAll, dbGet } = require('./db/index');`
- ❌ 禁止：`new sqlite3.Database()`、自行实例化

### 🔴 数据库写入

- ✅ `await enqueueRun('INSERT ...', [...])`
- ✅ `await runInTransaction(async (tx) => { ... })`
- ❌ 禁止：`db.run('BEGIN TRANSACTION')`、裸开事务

### 🔴 页面显示规范

- ✅ 页面显示助教工号：`{{ employee_id }}` 或 `${employee_id}`
- ❌ 禁止：在页面显示 `coach_no`（如 `{{ coach_no }}`、`${c.coach_no}`）
- ❌ 禁止：使用回退逻辑 `employee_id || coach_no`（可能暴露系统编号）
- ✅ `coach_no` 仅限内部用途：API 参数、`:key` 绑定、`data-*` 属性、JS 内部逻辑

## 工作目录

所有设计/代码产出写入指定工作目录。

## 输出要求

- 设计方案：写入 `design.md`
- 代码实现：直接修改项目代码，提交Git
- 修复记录：写入工作目录的 `fix-log.md`

