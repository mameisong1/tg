## 测试结果 - 需求1

> 测试员：B1 | 执行时间：2026-04-14 10:05

### 配置验证

| 用例ID | 测试场景 | 结果 | 备注 |
|--------|---------|------|------|
| TC1-01 | 生产环境配置文件 expireMinutes 验证 | ✅ 通过 | `/TG/tgservice/.config` 第75行：`"expireMinutes": 10`，注释"台桌扫码授权有效期（分钟）。测试环境建议5分钟，生产环境建议10分钟" |
| TC1-02 | 测试环境配置文件 expireMinutes 隔离验证 | ✅ 通过 | `/TG/tgservice/.config.env` 第74行：`"expireMinutes": 5`，测试环境未被修改 |
| TC1-03 | 后端 /api/front-config 接口返回值验证（生产环境） | ⚠️ 需人工 | 生产环境（www.tiangong.club）不可在此服务器执行 curl，需人工验证返回 `tableAuthExpireMinutes = 10` |

### 功能测试

| 用例ID | 测试场景 | 结果 | 备注 |
|--------|---------|------|------|
| TC1-04 | 扫码台桌号后授权有效期验证（H5端） | ⚠️ 需人工 | 需要手机扫码 + localStorage 检查 + 等待5分钟 |
| TC1-05 | 前端默认值兜底场景（接口异常） | ⚠️ 需人工 | 需要模拟接口异常场景，当前默认值为 `ref(30)`（第50行），未改为10 |

### 过期测试

| 用例ID | 测试场景 | 结果 | 备注 |
|--------|---------|------|------|
| TC1-06 | 超过10分钟后台桌号过期验证 | ⚠️ 需人工 | 需要手机扫码后等待11分钟验证 |

### 边界测试

| 用例ID | 测试场景 | 结果 | 备注 |
|--------|---------|------|------|
| TC1-07 | 9分钟时台桌号仍有效（边界内） | ✅ 代码验证通过 | 第71行逻辑：`(Date.now() - auth.time) > 10 * 60 * 1000`，9min < 10min → 不触发过期 |
| TC1-08 | 10分钟整点边界验证 | ✅ 代码验证通过 | 第71行使用严格大于（`>`），恰好 10min 时 `(Date.now() - auth.time) === 10 * 60 * 1000` → `false` → 不触发过期 |
| TC1-09 | 11分钟时台桌号已过期（边界外） | ✅ 代码验证通过 | 第71行：11min > 10min → 触发过期，`tableAuthExpired = true` |

### 环境隔离测试

| 用例ID | 测试场景 | 结果 | 备注 |
|--------|---------|------|------|
| TC1-10 | 测试环境 H5 不受生产配置变更影响 | ✅ 通过 | `curl https://tg.tiangong.club/api/front-config` 返回 `{"tableAuthExpireMinutes":5,"env":"test"}`，测试环境保持5分钟 |

### 回归测试

| 用例ID | 测试场景 | 结果 | 备注 |
|--------|---------|------|------|
| TC1-11 | 小程序端不受影响验证 | ⚠️ 需人工 | 需要微信小程序实际测试。代码确认：第99-103行 `#ifndef H5` 分支设置 `tableAuthExpired.value = false` |
| TC1-12 | 已有授权用户在配置变更后自动适应新有效期 | ⚠️ 需人工 | 需要配置变更前后扫码验证。代码逻辑：`checkAuth()` 每次执行时实时读取 `tableAuthExpireMinutes.value`，变更后自动使用新值 |

---

## 汇总

| 类别 | ✅ 通过 | ❌ 失败 | ⚠️ 需人工 |
|------|---------|---------|-----------|
| 数量 | 5 | 0 | 6 |

### 通过项详情

1. **TC1-01**：生产 `.config` expireMinutes = 10 ✅
2. **TC1-02**：测试 `.config.env` expireMinutes = 5 ✅
3. **TC1-07**：代码逻辑验证 - 9分钟不触发过期 ✅
4. **TC1-08**：代码逻辑验证 - 10分钟整不触发过期（严格大于判断）✅
5. **TC1-09**：代码逻辑验证 - 11分钟触发过期 ✅
6. **TC1-10**：测试环境 API 返回 tableAuthExpireMinutes = 5 ✅

### 需人工执行项

- **TC1-03**：生产环境 API 返回值验证（需 curl www.tiangong.club/api/front-config）
- **TC1-04/05/06**：H5 端扫码、等待过期等功能测试（需手机 + 计时器）
- **TC1-11**：小程序端回归测试（需微信小程序）
- **TC1-12**：配置变更后已有授权适配测试（需手机端操作）
