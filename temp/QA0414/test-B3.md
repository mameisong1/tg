## 测试用例 - 需求3：前台H5商品一览搜索功能 + 分类筛选优化

> **测试员**：B3
> **日期**：2026-04-14
> **设计稿**：design-A3.md
> **涉及文件**：`/TG/tgservice-uniapp/src/pages/products/products.vue`
> **环境**：H5（`tg.tiangong.club` 开发环境）

---

### 搜索功能测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC3-01 | 输入已存在商品名称，验证搜索结果正确过滤 | H5商品点单页面已加载，商品列表中有"可乐"商品 | 1. 打开H5商品点单页面<br>2. 在搜索框输入"可乐"<br>3. 等待300ms防抖 | 1. 商品列表仅显示名称包含"可乐"的商品<br>2. 顶部显示"找到 X 件商品"提示<br>3. 不包含"可乐"的商品不显示 | 待执行 |
| TC3-02 | 输入不存在的商品名称，验证"未找到"提示 | H5商品点单页面已加载 | 1. 在搜索框输入"asdfghjkl"（不存在的关键词）<br>2. 等待300ms防抖 | 1. 商品列表区域显示空状态<br>2. 显示文案"未找到「asdfghjkl」相关商品"<br>3. 显示"清除搜索"按钮 | 待执行 |
| TC3-03 | 搜索框清空按钮功能 | 搜索框中已有搜索词（如"可乐"），商品列表已过滤 | 1. 点击搜索框右侧 ✕ 清除按钮 | 1. 搜索框内容清空<br>2. 商品列表恢复显示全部商品<br>3. "找到 X 件商品"提示消失<br>4. ✕ 清除按钮隐藏 | 待执行 |
| TC3-04 | 搜索+分类组合过滤 | 商品列表已加载，分类按钮显示"全部"、"酒水"等 | 1. 在搜索框输入"可乐"<br>2. 等待搜索结果出现<br>3. 点击"酒水"分类按钮 | 1. 分类按钮切换到"酒水"（金色选中态）<br>2. 商品列表同时满足：名称含"可乐" AND 分类为"酒水"<br>3. 若当前分类下无匹配商品，应自动切到"全部"再搜索（按设计稿6.2逻辑） | 待执行 |
| TC3-05 | 输入特殊字符搜索 | H5商品点单页面已加载 | 1. 在搜索框输入特殊字符如 `@#$%` 或 `<script>` 等<br>2. 等待300ms防抖 | 1. 页面不报错、不崩溃<br>2. 不触发 XSS 攻击<br>3. 若无匹配商品则显示"未找到"提示<br>4. 特殊字符按原样进行搜索匹配 | 待执行 |
| TC3-06 | 中文搜索 | 商品列表中有中文名称商品（如"冰红茶"、"矿泉水"等） | 1. 在搜索框输入中文"红茶"<br>2. 等待300ms防抖 | 1. 商品列表过滤出名称包含"红茶"的商品（如"冰红茶"）<br>2. 中文搜索与英文搜索行为一致 | 待执行 |
| TC3-07 | 搜索防抖验证（输入过程中不频繁触发过滤） | H5商品点单页面已加载，搜索框为空 | 1. 将鼠标/手指点击搜索框获得焦点<br>2. **快速连续输入 3 个字符**（如"abc"），每个字符间隔 **< 300ms**（可用打字方式快速输入）<br>3. 输入过程中观察商品列表是否发生变化<br>4. 停止输入，等待 **300ms**<br>5. 观察是否以完整关键词"abc"执行过滤 | 1. 输入过程中（300ms内），商品列表**不发生变化**（不会被中间词如"a"、"ab"触发过滤）<br>2. 停止输入约 300ms 后，才以完整关键词执行过滤<br>3. 验证要点：过滤动作应只在**最后一次输入后约 300ms** 触发一次，而不是每次按键都触发<br>4. 可通过浏览器控制台 `console.log` 或 Network 面板确认只发送了一次搜索请求 | 待执行 |
| TC3-16 | 搜索后清空再搜索（回归测试） | H5商品点单页面已加载，商品列表中有多种商品（如"可乐"、"雪碧"、"矿泉水"等） | 1. 在搜索框输入"可乐"<br>2. 等待300ms防抖，确认列表仅显示"可乐"相关商品<br>3. 点击搜索框右侧 ✕ 清除按钮，确认列表恢复全部商品<br>4. 在搜索框输入"雪碧"<br>5. 等待300ms防抖 | 1. 第2步后：仅显示"可乐"相关商品<br>2. 第3步后：列表恢复全部商品，搜索框为空<br>3. 第5步后：仅显示"雪碧"相关商品，不再显示"可乐"相关商品<br>4. 搜索结果正确切换到新关键词的匹配结果，无残留过滤状态 | 待执行 |

### 分类筛选测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC3-08 | 分类按钮从2行grid改为1行横向滚动 | H5商品点单页面已加载 | 1. 打开H5商品点单页面<br>2. 观察分类筛选区域布局 | 1. 分类按钮为单行排列（非2行grid）<br>2. 分类标签呈胶囊形状（border-radius: 16px）<br>3. 无emoji图标和数量徽章<br>4. 分类栏总高度约36px（vs 原~100px） | 待执行 |
| TC3-09 | 点击分类按钮正确切换选中态 | H5商品点单页面已加载，当前选中"全部" | 1. 点击"酒水"分类标签<br>2. 观察选中态变化<br>3. 再点击"全部"分类标签 | 1. 点击"酒水"后，"酒水"标签变为选中态，"全部"取消选中<br>2. 商品列表刷新为"酒水"分类商品<br>3. 点击"全部"后恢复选中"全部"，商品列表恢复全部 | 待执行 |
| TC3-10 | 横向滚动功能正常 | H5商品点单页面已加载，分类超过屏幕宽度 | 1. 用手指/鼠标在分类区域向左拖动<br>2. 观察是否可滚动查看所有分类<br>3. 滚动到最右侧 | 1. 分类标签可横向滚动<br>2. 滚动条隐藏（不显示滚动条）<br>3. 所有分类（全部、酒水、奶茶、饮料、高汤、零食、泡面、槟榔、小吃、其他）均可滚动到达<br>4. 滚动流畅无卡顿 | 待执行 |
| TC3-11 | 选中态样式验证（金色背景+金色边框+加粗） | H5商品点单页面已加载 | 1. 点击任意分类标签（如"奶茶"）使其选中<br>2. 使用浏览器开发者工具检查样式 | 1. 背景色：`rgba(212,175,55,0.2)`（金色半透明背景）<br>2. 边框色：`rgba(218,165,32,0.5)`（金色边框）<br>3. 文字色：`#d4af37`（金色）<br>4. 字重：`font-weight: 600`（加粗）<br>5. 未选中按钮保持默认样式 | 待执行 |

### UI测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC3-12 | 搜索栏+分类按钮版面布局 | H5商品点单页面已加载 | 1. 打开H5商品点单页面<br>2. 从上到下观察页面结构 | 1. 标题栏"商品点单"（44px）<br>2. 搜索栏（~40px）：🔍 + 输入框 + ✕<br>3. 搜索栏与分类栏之间有极淡分隔线 `rgba(255,255,255,0.05)`<br>4. 分类标签栏（~36px）：横向滚动<br>5. 台桌信息区域<br>6. 商品列表网格<br>7. 整体层次清晰，不拥挤 | 待执行 |
| TC3-13 | 分类按钮高度压缩验证 | H5商品点单页面已加载 | 1. 使用开发者工具测量分类栏容器高度<br>2. 对比修改前的~100px高度 | 1. 分类栏容器高度约36px（含padding）<br>2. 单个分类标签高度约30px<br>3. 相比优化前~100px，高度减少约64% | 待执行 |
| TC3-14 | 整体版面美观度验收 | H5商品点单页面已加载 | 1. 在不同屏幕宽度下（320px、375px、414px）查看页面<br>2. 检查搜索栏、分类栏、商品列表的整体协调性 | 1. 搜索栏在窄屏下不溢出<br>2. 分类标签在窄屏下可正常横向滚动<br>3. 商品卡片排列整齐<br>4. 搜索栏背景色 `rgba(255,255,255,0.06)` 与页面整体深色风格协调<br>5. 金色选中态与项目整体风格统一 | 待执行 |

### 兼容性测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC3-15 | 小程序端不受影响（条件编译验证） | 小程序端已构建并运行 | 1. 在微信开发者工具中打开小程序端商品点单页面<br>2. 观察页面布局和功能 | 1. 搜索栏**不显示**（搜索功能仅在 `#ifdef H5` 区块内）<br>2. 分类按钮仍为原有的2行grid布局（在 `#ifndef H5` 区块内）<br>3. 分类按钮的emoji图标和数量徽章仍显示<br>4. 商品列表正常显示，无报错<br>5. 分类切换功能正常<br>6. 若 `filteredProducts` 计算属性被引入，小程序端也应正确渲染（无搜索词时等同于products） | 待执行 |

### 移动端测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC3-17 | 移动端软键盘弹出时搜索栏不被遮挡 | H5商品点单页面已在移动设备或移动端模拟模式下加载 | 1. 在移动设备上打开 H5 商品点单页面（或在 Chrome DevTools 中切换到移动端模式，如 iPhone 12 / Pixel 5）<br>2. 点击搜索框输入区域，触发软键盘弹出<br>3. 观察页面布局变化<br>4. 在软键盘弹出状态下输入搜索关键词<br>5. 尝试上下滚动商品列表 | 1. 软键盘弹出后，搜索栏**不被软键盘遮挡**，始终可见<br>2. 页面自动调整布局，商品列表区域可正常滚动<br>3. 输入搜索词后，过滤结果正常显示在可视区域内<br>4. 关闭软键盘后，页面恢复正常布局，无异常留白或错位 | 待执行 |

---

### 测试用例统计

| 分类 | 用例数 |
|------|--------|
| 搜索功能 | TC3-01 ~ TC3-07, TC3-16（8条） |
| 分类筛选 | TC3-08 ~ TC3-11（4条） |
| UI测试 | TC3-12 ~ TC3-14（3条） |
| 兼容性 | TC3-15（1条） |
| 移动端 | TC3-17（1条） |
| **合计** | **17条** |

---

### 测试环境要求

- **H5端**：Chrome 浏览器（桌面/移动端模拟）或 VNC Chrome（端口 9222）
- **小程序端**：微信开发者工具
- **移动端真机**：iOS Safari / Android Chrome（用于 TC3-17 软键盘测试）
- **测试账号**：使用已有台桌授权（5分钟有效）
- **后端服务**：开发环境 `tg.tiangong.club:8088`

### 注意事项

1. TC3-07 防抖测试需要精确计时，可用 `performance.now()` 辅助验证；关键验证点：快速连续输入 < 300ms 间隔的字符，确认过滤只在最后一次输入后约 300ms 触发一次
2. TC3-11 样式验证建议使用 Chrome DevTools Computed 面板检查
3. TC3-15 需在小程序端单独验证，不能仅依赖代码审查
4. TC3-16 为回归测试用例，重点验证搜索状态切换是否干净，无残留过滤
5. TC3-17 建议在真机上执行，Chrome DevTools 移动端模拟可作为补充验证
6. 所有搜索相关测试应在同一分类（建议"全部"）下进行，避免分类干扰
