## 测试用例 - 需求2

> **编写人**：测试员B2 | **日期**：2026-04-14  
> **对应设计稿**：/TG/temp/QA0414/design-A2-revised.md  
> **测试环境**：开发环境 `tg.tiangong.club`

### 一、上传功能测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC2-01 | 乐捐报备-上传1张图片 | 登录H5，进入乐捐报备页面 | 1. 填写日期、时长等必填字段<br>2. 点击上传图片<br>3. 从相册选择1张图片<br>4. 等待上传完成<br>5. 点击提交 | 1. 图片上传成功，缩略图显示在页面上<br>2. 提交成功，后端 `applications` 表 `images` 字段存 `'["url1"]'`<br>3. 页面提示提交成功 | 待执行 |
| TC2-02 | 乐捐报备-上传2张图片 | 同上 | 1. 填写必填字段<br>2. 点击上传图片，选择1张，等待上传完成<br>3. 再次点击上传图片（"+"按钮），选择第2张，等待上传完成<br>4. 点击提交 | 1. 两张图片缩略图均正常显示<br>2. 提交成功，`images` 字段存 `'["url1","url2"]'` | 待执行 |
| TC2-03 | 乐捐报备-上传3张图片（最大数量） | 同上 | 1. 填写必填字段<br>2. 依次上传3张图片<br>3. 点击提交 | 1. 三张图片缩略图均正常显示<br>2. 提交成功，`images` 字段存 `'["url1","url2","url3"]'`<br>3. 上传按钮消失或置灰（达到上限） | 待执行 |
| TC2-04 | 公休申请-上传1/2/3张图片 | 登录H5，进入公休申请页面 | 1. 填写请假备注等必填字段<br>2. 依次上传1张、2张、3张图片<br>3. 点击提交 | 1. 图片正常上传并显示缩略图<br>2. `images` 字段正确存储对应数量的URL<br>3. 提交成功 | 待执行 |
| TC2-05 | 请假申请-上传1/2/3张图片 | 登录H5，进入请假申请页面 | 1. 填写请假原因等必填字段<br>2. 依次上传1张、2张、3张图片<br>3. 点击提交 | 1. 图片正常上传并显示缩略图<br>2. `images` 字段正确存储对应数量的URL<br>3. 提交成功 | 待执行 |
| TC2-06 | 约客记录上传-上传1/2/3张图片 | 登录H5，进入约客记录上传页面，扫描台桌二维码或输入助教信息 | 1. 选择班次<br>2. 依次上传1张、2张、3张约客截图<br>3. 点击提交 | 1. 图片正常上传并显示缩略图<br>2. `guest_invitation_results` 表 `images` 字段正确存储<br>3. 提交成功后图片清空（调用 `clearAll()`） | 待执行 |
| TC2-07 | 尝试上传第4张图片（超限阻止） | 已在任一上传页面（乐捐/公休/请假/约客）上传了3张图片 | 1. 尝试再次点击上传按钮（"+"按钮） | 1. 上传按钮应不可点击或已隐藏<br>2. 或弹出提示"最多上传3张图片"<br>3. 不应触发 `uni.chooseImage` | 待执行 |
| TC2-08 | 上传过程中取消选择 | 在任一上传页面 | 1. 点击上传图片<br>2. 在系统相册选择器中点击取消 | 1. 不触发上传流程<br>2. 页面无报错<br>3. 已有图片不受影响 | 待执行 |

### 二、上传过程测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC2-09 | 上传进度条显示 | 在任一上传页面（建议使用大图片，>2MB） | 1. 点击上传图片，选择一张较大的图片<br>2. 观察上传过程中的进度条 | 1. 上传开始时显示进度条（或进度文本）<br>2. 进度从 0% 逐步增长到 100%<br>3. 上传完成后进度条消失或显示"上传成功" | 待执行 |
| TC2-10 | 上传失败-网络异常 | 在任一上传页面 | 1. 断开网络（或关闭WiFi/数据）<br>2. 点击上传图片，选择一张图片<br>3. 观察错误提示 | 1. 上传失败后弹出错误提示（如"上传失败，请重试"）<br>2. 进度条回到初始状态<br>3. 页面不崩溃，可重新尝试上传 | 待执行 |
| TC2-11 | 上传失败-OSS签名过期 | 在上传页面停留超过签名有效期（约15分钟） | 1. 打开上传页面，等待15分钟以上<br>2. 点击上传图片，选择一张图片<br>3. 观察结果 | 1. 如签名过期，应自动获取新签名后上传<br>2. 或提示"签名过期，请刷新页面后重试"<br>3. 不应出现500错误 | 待执行 |
| TC2-12 | 已上传图片的删除功能 | 在任一上传页面，已上传2张图片 | 1. 点击第一张图片上的删除按钮（×）<br>2. 确认删除 | 1. 第一张图片从页面移除<br>2. 剩余1张图片正常显示<br>3. `imageUrls` 数组正确更新<br>4. 上传按钮恢复可用 | 待执行 |
| TC2-13 | 删除全部图片后重新上传 | 在任一上传页面，已上传3张图片 | 1. 依次删除所有3张图片<br>2. 重新点击上传按钮<br>3. 选择2张图片上传 | 1. 所有图片删除后页面清空<br>2. 重新上传成功<br>3. `imageUrls` 数组正确更新为新上传的2张图片 | 待执行 |
| TC2-14 | 上传非图片文件 | 在任一上传页面 | 1. 点击上传图片<br>2. 尝试选择一个非图片文件（如 .pdf / .doc） | 1. 系统相册应自动过滤，只显示图片文件<br>2. 如选择了非图片文件，应提示"请选择图片文件" | 待执行 |

### 三、展示功能测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC2-15 | 加班审批-显示多张图片 | 1. 已提交一条包含3张图片的加班申请<br>2. 登录后台管理 | 1. 进入加班审批页面<br>2. 找到该条申请记录<br>3. 查看图片展示区域 | 1. 3张图片以横向滚动方式展示（`.image-scroll`）<br>2. 点击图片可进入 `uni.previewImage` 全屏预览<br>3. 无图片的记录不显示图片区域 | 待执行 |
| TC2-16 | 公休审批-显示多张图片 | 1. 已提交一条包含3张图片的公休申请<br>2. 登录后台管理 | 1. 进入公休审批页面<br>2. 找到该条申请记录<br>3. 查看图片展示区域 | 1. 3张图片以横向滚动方式展示<br>2. 点击图片可全屏预览<br>3. 无图片的记录不显示图片区域 | 待执行 |
| TC2-17 | 乐捐一览-显示多张图片 | 1. 已提交一条包含3张图片的乐捐报备<br>2. 登录H5进入乐捐一览 | 1. 找到该条乐捐记录<br>2. 查看图片展示区域 | 1. 3张图片以 `aspectFill` 模式展示（`.image-scroll`）<br>2. 点击图片可全屏预览<br>3. 此前无图片展示的乐捐一览现在应显示图片 | 待执行 |
| TC2-18 | 约客审查-列表显示多张图片 | 1. 已上传一条包含3张图片的约客记录<br>2. 登录后台管理进入约客审查 | 1. 在列表中找到该条约客记录<br>2. 查看图片缩略图区域 | 1. 3张图片以网格形式展示（`.card-image-grid`）<br>2. 点击任一张缩略图可打开审查弹窗，且从该图片开始查看<br>3. 图片最多显示3张（`.slice(0, 3)`） | 待执行 |
| TC2-19 | 约客审查-单张图片的记录仍正常显示 | 1. 执行迁移脚本前已有一条只有 `invitation_image_url` 的旧记录<br>2. 执行迁移脚本 | 1. 在约客审查列表找到该旧记录<br>2. 点击进入审查弹窗 | 1. 迁移后 `images` 字段应为 `'["旧URL"]'`<br>2. 审查弹窗能正常显示该图片<br>3. 有效/无效按钮正常工作 | 待执行 |

### 四、UI测试（约客审查弹窗改造）

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC2-20 | 约客审查-图片占90%屏幕 | 1. 有一条包含图片的约客记录<br>2. 进入约客审查页面 | 1. 点击该记录打开审查弹窗<br>2. 观察图片大小 | 1. 图片 `max-height` 应为 85vh（设计稿要求，近似90%屏幕）<br>2. 图片宽度应为 90vw<br>3. 相比改造前（50vh）明显变大<br>4. 图片清晰可辨，文字可读 | 待执行 |
| TC2-21 | 约客审查-有效/无效按钮缩小 | 同上 | 1. 打开审查弹窗<br>2. 观察底部有效/无效按钮 | 1. 按钮 `height` 应为 36px（原44px）<br>2. 按钮 `font-size` 应为 13px（原15px）<br>3. 按钮不遮挡图片<br>4. 按钮仍可正常点击 | 待执行 |
| TC2-22 | 约客审查-多图指示器 | 有一条包含3张图片的约客记录 | 1. 打开审查弹窗<br>2. 观察图片下方的指示器 | 1. 显示当前图片序号，如 "1/3"<br>2. 显示圆点指示器（如 ● ○ ○）<br>3. 切换图片时指示器同步更新 | 待执行 |
| TC2-23 | 约客审查-左右切换图片 | 有一条包含3张图片的约客记录 | 1. 打开审查弹窗<br>2. 点击右侧箭头（›）切换到下一张<br>3. 点击左侧箭头（‹）切换到上一张<br>4. 在第一张时点击左侧箭头<br>5. 在最后一张时点击右侧箭头 | 1. 右箭头切换到下一张，指示器更新<br>2. 左箭头切换到上一张，指示器更新<br>3. 在第一张时左箭头无反应（或禁用）<br>4. 在最后一张时右箭头无反应（或禁用） | 待执行 |
| TC2-24 | 约客审查-点击助教名可全屏预览 | 有一条包含3张图片的约客记录 | 1. 打开审查弹窗<br>2. 点击助教名称区域 | 1. 触发 `uni.previewImage` 全屏预览<br>2. 可从当前图片开始滑动浏览所有图片 | 待执行 |

### 五、数据迁移测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC2-25 | 迁移脚本-正常执行（测试环境） | 1. 数据库中有包含 `proof_image_url` 和 `invitation_image_url` 的旧数据<br>2. 数据库中尚无 `images` 列 | 1. 在测试环境执行：`cd /TG/tgservice && node backend/migrations/migrate-images-to-array.js`<br>2. 观察控制台输出 | 1. 控制台显示迁移过程：[1/4] 新增列 → [2/4] 迁移applications → [3/4] 新增列 → [4/4] 迁移guest表<br>2. 显示迁移统计信息<br>3. 数据一致性检查显示 ✅ | 待执行 |
| TC2-26 | 迁移脚本-旧数据正确迁移 | 同上 | 1. 执行迁移脚本<br>2. 检查数据库中 `applications` 表的 `images` 字段<br>3. 检查 `guest_invitation_results` 表的 `images` 字段 | 1. 原 `proof_image_url` 非空的行，`images` 应为 `'["原URL"]'`（JSON数组格式）<br>2. 原 `invitation_image_url` 非空的行，`images` 应为 `'["原URL"]'`<br>3. 原为空的行，`images` 仍为 `NULL` | 待执行 |
| TC2-27 | 迁移脚本-幂等性（执行两次不报错） | 已成功执行过一次迁移脚本 | 1. 再次执行迁移脚本<br>2. 观察控制台输出和退出码 | 1. 脚本不报错，正常退出（exit code 0）<br>2. 控制台显示 "images 列已存在，跳过 ALTER"<br>3. 数据不被重复迁移（UPDATE 的 changes = 0 或不重复写入）<br>4. 数据统计与第一次执行一致 | 待执行 |
| TC2-28 | 迁移脚本-指定数据库路径 | 无 | 1. 执行：`cd /TG/tgservice && node backend/migrations/migrate-images-to-array.js /TG/tgservice/db/tgservice.db`<br>2. 观察输出 | 1. 脚本使用指定的数据库路径<br>2. 迁移过程正常<br>3. 输出中显示正确的数据库路径 | 待执行 |
| TC2-29 | 迁移脚本-无旧数据时执行 | 数据库中无 `proof_image_url` 和 `invitation_image_url` 数据（全新数据库） | 1. 执行迁移脚本 | 1. 不报错<br>2. migrations 的 changes = 0<br>3. `images` 列已创建但全部为 NULL | 待执行 |

### 六、公共模块测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC2-30 | 公共模块-image-upload.js 存在且可导入 | 前端代码已部署 | 1. 检查文件 `src/utils/image-upload.js` 是否存在<br>2. 在4个上传页面中确认引入了该模块 | 1. 文件存在且代码完整<br>2. lejuan.vue、leave-apply.vue、overtime-apply.vue、invitation-upload.vue 均通过 `import { useImageUpload }` 引入<br>3. 不再有各自独立的 `uploadImage()` / `uploadFile()` 函数 | 待执行 |
| TC2-31 | 公共模块-maxCount 参数生效 | 前端代码已部署 | 1. 检查各页面调用 `useImageUpload()` 时的参数 | 1. 4个页面均传入 `maxCount: 3`<br>2. 各页面传入不同的 `errorType`（lejuan_proof / leave_proof / overtime_proof / invitation_screenshot）<br>3. 各页面传入相同的 `ossDir: 'TgTemp/'` | 待执行 |
| TC2-32 | 公共模块-removeImage 功能 | 在任一上传页面上传了3张图片 | 1. 点击第2张图片的删除按钮<br>2. 观察剩余图片和数组状态 | 1. 第2张图片被移除<br>2. 剩余第1和第3张图片<br>3. 上传按钮恢复可用（当前数量 < maxCount） | 待执行 |
| TC2-33 | 公共模块-clearAll 功能 | 在约客记录上传页面上传了3张图片并提交 | 1. 提交成功后观察图片列表 | 1. 调用 `clearAll()` 后所有图片清空<br>2. `imageUrls` 数组为空<br>3. 页面恢复初始状态，可再次上传 | 待执行 |

### 七、后端 API 测试

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|---------|---------|
| TC2-34 | 后端-applications.js 接收 images 字段 | 后端代码已部署，迁移脚本已执行 | 1. 通过 API 提交一条乐捐报备，`body.images` 为 `'["url1","url2"]'`<br>2. 查询数据库该记录 | 1. 插入成功<br>2. `applications` 表 `images` 字段值为 `'["url1","url2"]'`<br>3. `proof_image_url` 为 NULL（不再写入） | 待执行 |
| TC2-35 | 后端-guest-invitations.js 接收 images 字段 | 同上 | 1. 通过 API 提交一条约客记录，`body.images` 为 `'["url1","url2","url3"]'`<br>2. 查询数据库该记录 | 1. 插入成功<br>2. `guest_invitation_results` 表 `images` 字段值正确<br>3. `invitation_image_url` 为 NULL（不再写入） | 待执行 |
| TC2-36 | 后端-查询接口返回 images 字段 | 数据库中有含 `images` 数据的记录 | 1. 调用 GET /api/applications 查询列表<br>2. 调用 GET /api/guest-invitations 查询列表 | 1. 返回数据中包含 `images` 字段<br>2. `images` 值为 JSON 数组字符串<br>3. 前端可正确 `JSON.parse` 解析 | 待执行 |

---

## 测试用例汇总

| 类别 | 用例数 | 用例ID范围 |
|------|--------|-----------|
| 上传功能测试 | 8 | TC2-01 ~ TC2-08 |
| 上传过程测试 | 6 | TC2-09 ~ TC2-14 |
| 展示功能测试 | 5 | TC2-15 ~ TC2-19 |
| UI测试（约客审查） | 5 | TC2-20 ~ TC2-24 |
| 数据迁移测试 | 5 | TC2-25 ~ TC2-29 |
| 公共模块测试 | 4 | TC2-30 ~ TC2-33 |
| 后端 API 测试 | 3 | TC2-34 ~ TC2-36 |
| **合计** | **36** | |

---

## 执行说明

1. **执行顺序建议**：TC2-25~29（迁移）→ TC2-30~33（公共模块）→ TC2-34~36（后端API）→ TC2-01~14（上传）→ TC2-15~24（展示+UI）
2. **迁移脚本必须在代码部署前执行**，否则新代码读取 `images` 字段时该列可能不存在
3. **测试环境数据库路径**：`/TG/tgservice/db/tgservice.db`
4. **所有"实际结果"列**在执行后填写：✅ 通过 / ❌ 失败（附备注）
